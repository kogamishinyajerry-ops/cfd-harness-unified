# case_003 CRM-HLS · Ramp Log · Session 1 (2026-05-11)

**Substrate-listening test**: drive case_003 through main-project preprocessing
primitives (A1 + P0) inside `ui/backend/user_drafts/imported/case_003_crm_hls/`
rather than desktop sandbox. Goal: see what works, what's the next blocker,
what new V-series findings surface.

**Driver**: `scripts/case_003/ramp_session_1.py` + `_freecad_probe.py`
**Step source**: copied from `~/Desktop/case_003_crm_hls_boundary_layer/inputs/cad_codex_v1.step` (1.96 MB; Codex-built v1)
**Workspace**: `ui/backend/user_drafts/imported/case_003_crm_hls/` (gitignored)

---

## Mechanical results

| Step | Result | Evidence |
|---|---|---|
| P0 STEP header parse | ✅ PASS — declared `mm` via `SI_UNIT(.MILLI.,.METRE.)` | `parse_step_header_unit()` returned `GeometricUnit.MM` |
| A1 STEP load (FreeCAD subprocess) | ✅ PASS — 10 named leaf solids preserved | probe JSON: airframe_reference, root_mount_pad, root_mount_cover, thin_access_plate, inlet, outlet, symmetry_plane, farfield_top/bottom/outer |
| P0 combined decision | ⚠️ FLAGGED UNKNOWN (correctly per current heuristic) | bbox max extent 2.438×10⁶ mm (= 2438 m, farfield boundary) not plausible under any unit; declared MM contradicts → WARNING |

---

## Substrate-listening findings (4 new, 1 V-series reclassification)

### F-NEW-1 · A1 mechanical extraction validated on second non-APU industrial case
A1's `Import.insert()` pattern preserved Codex-injected body labels through
a FreeCAD subprocess bridge. Confirms A1 is reusable beyond the APU bay
source. **Status**: A1 advisor → field-validated, second case.

### F-NEW-2 · FreeCAD subprocess bridge has positional-arg friction
`freecadcmd` auto-opens any positional argument as "file to open", which
clashes with passing script paths/IO paths via argv. Workaround: sidecar
args file (`_probe_args.txt`) at a known path. Works but is fragile. If
this bridge pattern persists across future sessions, consider a proper
subprocess wrapper service in `geometry_ingest/` (next-spike candidate).

### F-NEW-3 · P0 heuristic conflates airframe-class bodies with CFD domain bodies
When STEP contains both the airframe AND its CFD domain boundaries
(inlet/outlet/farfield), the bbox max extent reflects the **domain**, not
the geometry of interest. Result: P0 flags UNKNOWN even when airframe
bodies themselves are at sensible mm-scale.

Per-body extents in case_003:
- `airframe_reference`: 182.88 m × 91.44 m × 182.88 m (large but not catastrophic)
- `root_mount_pad`: 18.29 m × 1.91 m × 3.05 m
- `thin_access_plate`: 27.43 m × 3.43 m × **0.0008 m** (defect; matches profile D8 = 0.80 mm)
- `inlet`/`outlet`: 0.152 m × 1600 m × 1541 m (CFD domain walls)
- `farfield_*`: 2438 m × 1600 m × 0.152 m (CFD domain top/bottom/outer)
- `symmetry_plane`: 2438 m × 0.152 m × 1541 m

**Spike candidate**: extend P0 to accept a `body_class_filter` callback or
take a list of "airframe bodies vs domain bodies". Or downstream caller
runs P0 only on airframe-class bodies. **Estimate**: ≤40 LOC + test.

### F-NEW-4 · V20 reclassification — case_003 v1 finding likely misdiagnosis
case_003 v1 reported "loaded as 91 m, ~25.4× over expected" suggesting
inch-treated-as-mm. Session 1 evidence contradicts:

- STEP header explicitly declares `SI_UNIT(.MILLI.,.METRE.)` — MM intentional
- Airframe x_length = 91.44 m → bbox is **exactly 3600 inches** (clean number, not artifact)
- Real CRM-HLS half-span ≈ 29.4 m; loaded 91.44 m / 29.4 m ≈ **3.11×** (not 25.4×)
- 25.4× would imply factor of 1 inch = 25.4 mm; observed factor is ~3×

**Hypothesis**: Codex's `build_cad.py` script scaled the airframe by ~3× to
fit a larger CFD domain (or for a different demo purpose). The "V20 unit
issue" is actually a **deliberate scaling artifact**, not a unit-conversion
bug.

**V-series action**: open V20 amendment in `industrial_case_solver_findings.md`:
status = "misdiagnosis confirmed by session 1 probe; actual issue is intentional
3× scale in Codex CAD generator, not unit chain bug".

### F-NEW-5 · Workbench route layer does NOT yet support STEP import
Confirmed: `ui/backend/routes/import_geometry.py` accepts STL only (per M5.0
charter). For case_003 to fully ride the workbench shell, we need either
(a) STEP→STL conversion as a pre-route step, or (b) extend the import
route to accept STEP. Per V198 §S3 substrate philosophy, recommend (a) —
add a `freecad_step_to_stl` helper in `geometry_ingest/` that wraps
freecadcmd to emit per-body STL, then standard STL import path handles
the rest.

---

## What this session did NOT cover (deferred)

- Conversion STEP → per-body STL via FreeCAD subprocess (next session)
- Workbench import endpoint hit + manifest write
- snappyHexMesh ramp + cell budget validation
- BC writer + simpleFoam run

---

## Predicted next blockers (ordered)

1. **STL conversion bridge**: need `freecad_step_to_stl(step_path, out_dir)` for case_003 to enter workbench. Spike-class. (Phase 4a)
2. **P0 body-class awareness**: fix F-NEW-3. Spike-class. (Phase 4b)
3. **sHM cell budget for external aero**: 91 m semi-span + 1.5 km farfield → cell count blow-up risk under default N2.1 sizing. May require N2 advisor expansion. (Phase 4c)
4. **Inlet/outlet/farfield/symmetry patch type assignment**: case_003 has these as named bodies in STEP, but workbench patch classification (post-sHM) needs heuristic that maps these labels to patch types. P6 in preprocessing P-list. (Phase 5)

---

## V-series rows to update / add

| Action | Where |
|---|---|
| **V20 amend** — misdiagnosis confirmed | `industrial_case_solver_findings.md` |
| **V25 (NEW)** — P0 heuristic doesn't separate airframe bodies from CFD domain bodies → false UNKNOWN on multi-class STEP | new row in V-series + cross-link to F-NEW-3 |
| **V26 (NEW)** — FreeCAD subprocess bridge has positional-arg auto-open quirk; sidecar args file workaround | new row in V-series + cross-link to F-NEW-2 |
| **V27 (NEW)** — workbench route layer accepts STL only; STEP requires pre-route conversion | new row in V-series + cross-link to F-NEW-5 |

(V-series rows not added in this session; logged here for next session to formalize.)

---

## confidence: high

- All mechanical claims verified by probe JSON (committed-as-evidence)
- V20 reclassification supported by 3 independent pieces of evidence (STEP header, exact-number bbox, ratio math)
- Predicted blockers grounded in current code state, not speculation
- Substrate-listening worked exactly as intended — surfaced new gaps without writing premature code

---

# case_003 · Session 2 (2026-05-11)

**Goal**: clear F-NEW-5 (workbench accepts STL only) by building the
STEP→STL bridge predicted as "Phase 4a next blocker".

## Deliverable

`ui/backend/services/geometry_ingest/freecad_step_to_stl.py` + sidecar
`_freecad_step_to_stl_sidecar.py`. Wraps `freecadcmd` subprocess (inherits
F-NEW-2 sidecar-args pattern), opens STEP via A1's `Import.insert()`,
meshes each named leaf solid via `MeshPart.meshFromShape` at default
Q3 deflections (lin=0.05 mm, ang=0.1 rad — STAR-CCM+ production default
from `cad-step-stl-prep` skill), emits one ASCII STL per body + manifest.

## Mechanical results (session 2)

| Metric | Result |
|---|---|
| STLs produced | 10 (matches probe session 1 named-leaf count) |
| Total facets | 393,498 |
| Airframe facets | 393,390 |
| Defect bodies (root_mount_pad / root_mount_cover / thin_access_plate) | 12 facets each |
| Domain bodies (inlet / outlet / farfield_* / symmetry_plane) | 12 facets each |
| Output format | ASCII STL (`solid Mesh ... endsolid`) |
| File size — airframe | 87 MB |
| File size — domain bodies | 3 KB each |
| Unit-test suite (geometry_ingest, 4 files) | 36 pass + 1 skip, 0 regression |

## Substrate-listening findings (session 2)

### F-NEW-6 · Defect bodies are flat primitives, not tessellated surfaces
`root_mount_pad`, `root_mount_cover`, `thin_access_plate` each meshed to
exactly 12 facets — meaning Codex's `build_cad.py` emitted them as
axis-aligned boxes (6 quads × 2 tris). The defect markers carry
**spatial position** but no surface curvature. For boundary-layer cell
ramping, this is fine (sHM treats them as patch surfaces); for any
"defect aerodynamic signature" experiment we'd need a richer CAD source.
**Status**: noted, no spike action yet.

### F-NEW-7 · 87 MB airframe ASCII STL is workbench-importable but heavy
At Q3 deflection (0.05 mm linear) on a 91-m-class airframe, facet count
is 393k. Within the M5.0 STL importer envelope but at the upper end.
sHM downstream will need careful refinement-level choices to avoid
cell-budget blow-up (per F-NEW-3 follow-on prediction in session 1).
**Spike candidate (later)**: `step_to_per_body_stl` could accept a
`facet_budget_per_body` hint that adjusts deflection per-body (airframe
loose, defects fine). Not needed now.

### F-NEW-8 · Bridge is provider-portable but FreeCAD-bound
`freecad_cmd` is parameterized (default `/Applications/FreeCAD.app/...`
for macOS); Linux callers pass `freecad_cmd="/usr/bin/freecadcmd"`. The
sidecar relies on `Import`/`MeshPart`/`Mesh` which are stable FreeCAD
modules. Containerized worker would mount FreeCAD AppImage. **Status**:
not blocking; flagged for any future deployment story.

## What this unblocks

- case_003 (and any STEP-source case) can now enter the workbench import
  path: STEP → `step_to_per_body_stl()` → 10× ASCII STL → existing
  `routes/import_geometry.py` accepts them.
- The "AI agent writes a CAD script" loop (Codex-built v1) integrates
  end-to-end: CAD generation → STL export → workbench import → sHM →
  solver.
- F-NEW-3 (P0 body-class filter) and patch classification (P6) become
  meaningful next questions because we now have real STL bodies to
  classify.

## What this did NOT cover

- Did **not** call `routes/import_geometry.py` with the new STLs (next session)
- Did **not** combine STL into single mesh — kept per-body to let
  downstream patch classification work
- Did **not** validate sHM cell budget on 91 m airframe yet
- Did **not** check trimesh load roundtrip on the ASCII output (assumed
  via `mesh.write(Format="AST")` correctness; trimesh-validation deferred)

## confidence: high

- 10 STL count matches probe session 1 expectation exactly
- ASCII format verified by `head -3` per file
- All 36 geometry_ingest tests green after addition
- Integration test gated on `os.path.exists(DEFAULT_FREECAD_CMD)` —
  green on macOS dev box, will skip on CI without FreeCAD (intentional)

---

# case_003 · Session 3 (2026-05-11) · workbench import probe

**Goal**: ride the 10 session-2 STLs through `POST /api/import/stl` (no
new code yet — observe what the existing M5.0 route does, where it
breaks, where it lies). Spike-class evidence run.

**Driver**: `scripts/case_003/ramp_session_3_workbench.py` (FastAPI
TestClient against `ui.backend.main:app`).

## Four probes

| Probe | Payload | Bytes | Status | Highlights |
|---|---|---|---|---|
| **A** raw airframe | `airframe_reference.stl` | 86.6 MB | **413** | `failing_check=size_limit`; cap = 50 MB |
| **B** single small body | `inlet.stl` raw | 3 KB | **200** | watertight=True; `patches=['defaultFaces']`; `all_default_faces=True`; `unit_guess='unknown'` |
| **C** combined 9 (no airframe) | rewritten `solid <label>` headers | 27 KB | **200** | watertight=True; `patches=['root_mount_pad', 'root_mount_cover', 'thin_access_plate', 'inlet', 'outlet', 'symmetry_plane', 'farfield_top', 'farfield_bottom', 'farfield_outer']`; `solid_count=9` |
| **D** combined all 10 | rewritten headers | 86.6 MB | **413** | `failing_check=size_limit` |

## Substrate-listening findings (6 new)

### F-NEW-9 · 50 MB STL cap blocks Q3-deflection airframe (P1 blocker)
`MAX_STL_BYTES = 50 MB` (route line 34) was spec-D for hand-authored STL
uploads. With FreeCAD Q3 tessellation (STAR-CCM+ production default)
the case_003 91-m airframe alone produces an 87 MB ASCII STL.

**Options** (no decision yet, substrate-first):
- (a) Raise cap to e.g. 200 MB for industrial substrate (+ rate-limit at
  proxy if abuse concern) — simplest, but masks the broader "should we
  store binary STL?" question
- (b) Bridge emits binary STL by default (3× smaller per facet) — but
  loses the multi-solid named-block round-trip that detect_patches needs
- (c) Bridge emits ASCII for label-bearing bodies + binary for label-less
  bodies + raises cap to 200 MB; combined output stays ASCII
- (d) Lower Q3 → Q2 (0.1 mm linear) only for airframe-class bodies →
  facet count drops ~4×, file ~22 MB — but loses production-grade
  surface fidelity for the very body that needs it

**Recommendation** (substrate evidence basis): raise cap to 200 MB +
keep ASCII (option a). Real industrial CAD at production tessellation
will routinely cross 50 MB; the cap was authored before APU bay / CRM
substrate experience. The named-block round-trip is load-bearing
(probe C proves it works) so binary STL is a regression.

### F-NEW-10 · Bridge needs a "combined multi-solid" emit mode
Probe C proves the round-trip works perfectly when ``solid <label>``
headers replace FreeCAD's literal ``solid Mesh``. The bridge today
emits per-body files only. Next-spike candidate: add an opt-in
``combined_path=`` parameter (or sibling helper) that produces one
multi-solid ASCII STL with body-label headers ready for direct workbench
upload.

Estimate: ~30 LOC + 1 test. Pure spike — no schema/contract change.

### F-NEW-11 · Single-body upload from raw FreeCAD output assigns defaultFaces (expected, not a bug)
FreeCAD's `mesh.write(Format="AST")` writes `solid Mesh` regardless of
body label. trimesh sees one solid → returns `Trimesh` (not Scene) →
`detect_patches` falls to single-Trimesh branch → `defaultFaces` patch
+ `all_default_faces=True` flag set. The route's warning is honest and
correct ("re-export with named solids per inlet/outlet/wall"). This
finding is documenting expected behavior; no code change wanted.

### F-NEW-12 · Unit guess UNKNOWN at workbench layer reflects F-NEW-3 P0 conflation
Probe C ingestion reported `unit_guess='unknown'`, `bbox_extent` ≈
2.44 M × 1.60 M × 1.54 M. The bbox is dominated by the CFD domain
(farfield/symmetry walls), exactly as session 1 F-NEW-3 predicted at
the P0 layer. Fix is the same: P0 / unit-guess should run on
airframe-class bodies, not on the full multi-class collection. **This
is the case where 4b is no longer "speculative cleanup" — it is the
direct fix for unit_guess UX at the workbench**.

### F-NEW-13 · "watertight=True" passes for a boundary-surfaces-only payload — misleading as a CFD-readiness signal
Probe C's combined STL is 9 boundary surfaces (3 defect patches + 6
domain box faces). After `merge_vertices()`, trimesh reports
`is_watertight=True` because the 6 domain faces topologically close
into a hexahedral hull (defects on top don't break closure). But a
CFD-ready case needs the airframe (obstacle) + domain (fluid region) —
the watertight check passes geometrically while the case is far from
CFD-ready.

This is a **truth-chain finding**: the route's watertight gate is
correct for STL hygiene but is read by downstream users as "ready for
sHM", which it is not. Either rename the check ("hull watertight") or
add a "cell-zones-present" follow-up gate at the M7 mesh stage.

**Not a route bug; a UX / Truth Chain copy issue**. Route correctly
warns "9 disconnected bodies; M7 mesh generation may need explicit
cell-zone setup" — that warning IS the right channel, just needs to
land prominently in UI.

### F-NEW-14 · Test scaffolds land in user_drafts/imported/ (gitignored, expected)
Probes B+C produced two real case scaffolds at
`imported_2026-05-11T10-17-02Z_{b4f6b0f7, 46cc70f7}/`. Gitignored under
`user_drafts/imported/`. No cleanup needed; useful as live evidence.

## What this session did NOT cover

- Did **not** run sHM on the scaffolded cases (next session — F-NEW-13
  validation: would sHM actually run on the 9-body combined STL? what
  cells does it produce? does the airframe-as-separate-upload story
  even work in the M7 mesh stage?)
- Did **not** trigger 4b P0 body-class filter (deferred until we
  observed the workbench-layer manifestation; now confirmed needed per
  F-NEW-12)
- Did **not** propose F-NEW-9 cap change as a sub-DEC — substrate
  evidence sufficient for now; sub-DEC writes when actually changing
  the route

## Predicted next blockers (updated)

1. **F-NEW-9 cap** — 50 MB cap. Direct unblock for case_003 + any
   industrial-fidelity STEP. **Promote to sub-DEC** when changing
   route. Estimate: ~3 LOC + 1 test.
2. **F-NEW-10 combined-emit mode for bridge** — spike-class. Estimate:
   ~30 LOC + 1 test. Could land immediately.
3. **F-NEW-12 unit-guess body-class awareness (= original 4b)** —
   now confirmed needed at workbench layer, not just P0. Spike-class
   (≤40 LOC + test on `unit_detector.py`). Could also land immediately.
4. **F-NEW-13 truth-chain copy** — defer to UI / M-CONTROL-RAIL work;
   no code action this session.

## V-series queue (updated since session 2)

| Action | Where |
|---|---|
| **V25** (NEW, session 1) — P0 multi-class STEP false UNKNOWN | not yet |
| **V25a** (extend session 3 finding) — symptom also surfaces at workbench `unit_guess='unknown'`; not just P0 layer | extend V25 row |
| **V26** (NEW, session 1) — freecadcmd positional-arg friction; sidecar workaround | not yet |
| **V27** (NEW, session 1) — workbench accepts STL only; STEP needs pre-route conversion | now LANDED by Phase 4a, V27 row can land as "RESOLVED-by-V199-bridge" |
| **V28** (NEW, session 3) — 50 MB STL cap blocks Q3 industrial CAD | not yet |
| **V29** (NEW, session 3) — FreeCAD `mesh.write(Format=AST)` writes generic `solid Mesh` header; bridge should rewrite to body label before combined-emit | not yet |
| **V30** (NEW, session 3) — `is_watertight=True` on boundary-surfaces-only STL passes route gate but is not a CFD-readiness signal | not yet |

(V rows still queued; not added to `industrial_case_solver_findings.md`
this session — see RESUME for prioritization.)

## confidence: high

- All 4 probes reproducible via committed driver script
- F-NEW-9 + F-NEW-10 + F-NEW-12 are all observable in single probe output
- F-NEW-13 has clear geometric explanation (trimesh `merge_vertices`
  closes the domain hull regardless of airframe presence)
- The substrate-first methodology paid off: we now have 3 prioritizable
  blockers grounded in actual route behavior, not speculation

---

## Session 3 follow-up · F-NEW-9 cap raised + new finding F-NEW-15

**After** raising `MAX_STL_BYTES` from 50 → 200 MB (spike commit), re-ran
airframe upload to verify the wall is actually down. It is. **But** the
re-probe surfaced an unanticipated finding that did not appear in any
prior probe.

### F-NEW-15 · Codex-built airframe is 2 disconnected bodies, not 1
`POST /api/import/stl` with `airframe_reference.stl` at 87 MB now
returns **200**:

```
solid_count=1      (FreeCAD wrote single `solid Mesh` header → defaultFaces, F-NEW-11)
face_count=393,390 (matches session 2 expectation exactly)
warnings: "STL contains 2 disconnected bodies"
```

The "2 disconnected bodies" warning is **new** — it comes from trimesh's
post-`merge_vertices` connected-components count. The airframe is one
STEP body (per session 1 probe) but its triangulation forms **2
geometrically disconnected components** in 3D.

**Hypothesis**: Codex's `build_cad.py` for case_003 v1 likely composed
the airframe from 2 separate solids (e.g. fuselage + tail, or upper +
lower wing surface) that were `union`'d in CADQuery but landed in the
STEP as a non-manifold/disconnected pair, or were never welded.

**This is the F-NEW-13 truth-chain finding flipped onto the airframe
itself**: a single named STEP body can still fail the "is this one
flowable solid?" CFD-readiness question. The route's
`disconnected_bodies` warning is honest; it is the user (or downstream
sHM) that has to act on it.

**No code action this session**: F-NEW-15 documents substrate behavior,
doesn't demand a route change. Two follow-up questions remain open:
1. Was case_003 v1 CAD intentionally disconnected (for a defect
   experiment we missed)? — needs reading the Codex CAD generator
2. Should the bridge or the route reject "disconnected airframe-class
   body" with a stronger signal than a warning? — depends on (1)

### Tally update

`MAX_STL_BYTES = 200 MB` (committed). 87 MB airframe rides the route
cleanly. F-NEW-11 + F-NEW-12 + F-NEW-15 all reconfirmed on the new
upload path. Next spike: F-NEW-10 (combined multi-solid emit) so the
10-body case can ride a single POST and recover real patch names.

## confidence: high (session 3 follow-up)

- 200 MB cap verified by live 87 MB upload returning 200
- F-NEW-15 disconnected-bodies count is reproducible (trimesh
  connected-components is deterministic per STL)
- No frontend / proxy / docs reference the 50 MB number — the "wall"
  really was just 1 constant deep

---

## Session 3 follow-up #2 · F-NEW-10 combined-emit mode landed

**Spike**: added `combine_per_body_stls(stl_dir, out_path=None)` to
`freecad_step_to_stl.py`. Pure-Python; reads each per-body STL, rewrites
the generic `solid Mesh` header to `solid <stem>`, concatenates.

**Test**: 1 unit test with mock 3-body fixture; verifies opening-header
regex anchored to start-of-line (test bug caught & fixed: `solid <name>`
matches as substring of `endsolid <name>` without anchor).

**End-to-end probe with real case_003**:

```
combined.stl: 86.6 MB · 393,498 total facets · 10 solid blocks
POST /api/import/stl → 200 OK
solid_count=10  watertight=True
patches=[airframe_reference, root_mount_pad, root_mount_cover,
         thin_access_plate, inlet, outlet, symmetry_plane,
         farfield_top, farfield_bottom, farfield_outer]
```

**All 10 body labels round-trip cleanly** through:
- FreeCAD `solid Mesh` write → `combine_per_body_stls` header rewrite
  → workbench `detect_patches` → 10 named patches ready for sHM region
  binding at M7

### F-NEW-16 (NEW) · `disconnected_bodies=11` on 10 named bodies — confirms F-NEW-15
The combined upload reports 11 disconnected components even though it
has 10 named bodies. Crossed with F-NEW-15 (airframe alone reports 2
disconnected components), the math closes: 9 small bodies are 1
component each + airframe is 2 components = 11. So F-NEW-15 is robust
across single-upload and combined-upload paths — the airframe is
genuinely 2 disconnected solids regardless of how it's packaged.

This further suggests F-NEW-15 is a property of the **CAD source**
(Codex's `build_cad.py` output), not the bridge or the workbench.

## confidence: high (session 3 follow-up #2)

- Combined STL parses correctly through M5.0 route — all 10 patches
  named, face counts match per-body totals exactly
- F-NEW-15 → F-NEW-16 arithmetic check (9+2=11) closes the
  disconnected-bodies hypothesis
- Spike-class scope: 21 LOC code + 25 LOC test, no schema change,
  no contract change, existing tests still green

---

## Session 3 follow-up #3 · F-NEW-12 body-class filter landed

**Spike**: extended `detect_unit()` in `unit_detector.py` with a new
optional `body_extents_raw: list[float] | None` parameter. When supplied,
bodies whose max-extent is implausible under every common unit
(mm/cm/m/inch) are discarded as "CFD-domain class"; the largest extent
among the survivors replaces the overall `bbox_max_extent_raw` for the
decision.

**Backward compat**: callers that don't pass `body_extents_raw` get
identical behavior to before. 22/22 existing + new tests green.

### Live case_003 evidence (real probe data, no synthetic mocks)

```
Before (no filter): decision=UNKNOWN  confidence=0.00
After  (filter on): decision=MM       confidence=1.00

7/10 bodies discarded:
  - 4 farfield walls (2438 m extent at mm = implausible)
  - inlet / outlet / symmetry_plane (1540-1600 m at mm = implausible)
  - airframe_reference (182.88 m at mm = exceeds 100 m industrial upper
    bound; Codex 3× scaling artifact — F-NEW-4 surfacing via filter)

3 bodies carry the decision:
  - root_mount_pad / root_mount_cover (18.29 m)
  - thin_access_plate (27.43 m)
```

### F-NEW-17 (NEW) · Body-class filter accidentally surfaces F-NEW-4
The 100 m industrial upper bound flags the Codex-scaled airframe as
domain-class. This is **substrate cross-talk**: a heuristic designed to
separate airframe from CFD-domain also separates "real-size airframe"
from "implausibly-scaled airframe". It's not a bug — it's a side
benefit. The filter degrades gracefully (3 defects suffice for unit
decision); but in a future case where ALL bodies are above range
(e.g., a ship deck 200 m × 200 m × 30 m), this filter would mis-fire.

Mitigation when needed: make the industrial range configurable per
case (M3 boundary-conditions phase or workbench case-settings panel).
Not needed for case_003.

### Why this matters for the workbench UX

The pre-fix path made the workbench `unit_guess='unknown'` warning
fire on every industrial multi-class STEP — engineers were told to
"set the unit explicitly" even when the STEP header was unambiguous.
Post-fix, the workbench can show `unit_guess='mm'` with confidence
1.0 + a clean evidence trail. The route layer doesn't even need a
change: the route calls `detect_unit` only with overall bbox today;
the bridge OR the route can pass `body_extents_raw` to get the
filtered decision. That wiring is the **next** step (out of this
spike's scope).

## confidence: high (session 3 follow-up #3)

- Body-class filter validated on real case_003 probe data, not
  synthetic numbers
- UNKNOWN → MM confidence-1.00 transition is the F-NEW-12 fix
- F-NEW-17 substrate cross-talk is documented as a known side benefit
- Spike-class scope: ~25 LOC code + 3 new tests + 0 contract break
  + 0 schema break + 0 backward-incompat (existing callers unaffected)
