# DEC-V61-049 Pilot — lid_driven_cavity novice walk

## Persona check
- I am reviewing this as a first-semester CFD graduate student, not as a mature CFD validation reviewer. I know the incompressible Navier-Stokes equations at the textbook level, have read the early Anderson CFD chapters, and have run OpenFOAM's `icoFoam/cavity` tutorial once. I recognize words like SIMPLE, PISO, `blockMesh`, `checkMesh`, `sampleDict`, Re, residuals, and centerline profiles, but I do not yet have reliable intuition for SIMPLE under-relaxation, GCI/Richardson extrapolation, stream-function post-processing, or how to defend a validation report against published data. My concrete goal is: can `/learn/lid_driven_cavity` teach me enough to reproduce the case, diagnose obvious failures, post-process the result, compare against Ghia 1982 in at least five meaningful ways, and write a 1000-word course report without inventing missing facts? On the current page, my answer is no.

## Step 1 findings — Reading the page
- 🟠 **Hero / physics bullets** — The page starts cleanly: square cavity, moving lid, no-slip walls, Re definition are all visible in the LDC entry (`ui/frontend/src/data/learnCases.ts:122-135`) and rendered in the Story tab (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:340-350`). My confusion is not the physical setup; it is the missing "from textbook to OpenFOAM" bridge. As a novice I still do not know the actual coordinate scale, whether the page wants a unit square or `convertToMeters 0.1`, and whether Re=100 is achieved by `nu=0.01` or `nu=0.001`. The backend generator uses `convertToMeters 0.1` and `nu=0.1/Re`, so Re=100 gives `nu=0.001` (`src/foam_agent_adapter.py:756-782`), while the workflow says `nu=0.01` for a 1x1 cavity (`ui/frontend/src/data/learnCases.ts:146`). Fix: show a boxed "dimensionless tutorial path" and "harness audit path" table, or pick one path and keep all commands consistent.
- 🟠 **Why validation matters** — The validation paragraph says Ghia 1982's 129x129 data is the gold standard and centerline matching decides whether the first CFD pass succeeded (`ui/frontend/src/data/learnCases.ts:135`). That is motivating, but as a student I immediately ask: what exact table, what axis, how many points, and what error norm? Those details are scattered later and partially conflict: the gold YAML says u_centerline is Table I interpolated to 17 y points (`knowledge/gold_standards/lid_driven_cavity.yaml:59-97`), the page's observable card says "Table II" for u_centerline (`ui/frontend/src/data/learnCases.ts:141`), and the public flow-field caption says Table I has both u and v profiles (`ui/frontend/src/data/flowFields.ts:21-24`). Fix: one mini "Ghia data map" should state: Table I = u(x=0.5,y), Table II = v(x,y=0.5), current harness compares only u.
- 🟠 **Physics intuition** — The three new paragraphs are valuable but too compressed for my level. Terms like "Taylor-Görtler 螺旋涡", "weak turbulence regime", "2D 稳态解仍然数学存在但实验里看到的是时均场", "benchmark protocol" appear in one block (`ui/frontend/src/data/learnCases.ts:136`). I can appreciate the story, but I do not know what the stream function is, how vortex center is measured, or why 2D mathematical steady state can coexist with 3D experimental unsteadiness. Fix: add a short glossary under this block for `primary vortex`, `secondary vortex`, `stream function ψ`, `steady 2D idealization`, and `Ghia table`.
- 🔴 **Common pitfall** — The pitfall says lid corner velocity singularity can cause divergence and suggests mesh refinement, relaxed residual criteria, or accepting small corner error (`ui/frontend/src/data/learnCases.ts:137`). I do not know how much residual relaxation is acceptable before the validation becomes dishonest. The troubleshooting later says residuals should drop below `1e-5` (`ui/frontend/src/data/learnCases.ts:147`), while the pitfall says "放宽残差判据" without a safe floor. Fix: say "do not loosen below X for report runs; if you loosen for exploration, label the run non-validation."
- 🔴 **Solver card** — The solver card assumes I understand SIMPLE, pressure correction, URF, GAMG, residual control, and `linearUpwind` in one paragraph (`ui/frontend/src/data/learnCases.ts:138`). Worse, it states `U≈0.7, p≈0.3`, but the actual generator writes `U 0.9` and `p 0.3` (`src/foam_agent_adapter.py:992-1001`). It also says `fvSchemes` uses `linearUpwind`, but the current generator uses `bounded Gauss limitedLinearV 1` (`src/foam_agent_adapter.py:907-912`). Fix: align the prose with the actual emitted case files, then add a "why these values are safe at Re=100" note.
- 🔴 **Mesh card** — The page says `ncx=ncy=40` is enough for the teaching run and the workflow repeats `(40 40 1)` (`ui/frontend/src/data/learnCases.ts:139`, `ui/frontend/src/data/learnCases.ts:144`), but the actual LDC generator emits `(129 129 1)` (`src/foam_agent_adapter.py:6826-6829`), the rendered contour title says 129x129, and the gold YAML marks 129x129 as the mesh info (`knowledge/gold_standards/lid_driven_cavity.yaml:98-103`). This is a blocker for reproduction because I do not know which grid my report is supposed to reproduce. Fix: choose "audit run = 129x129" as the primary path; keep 40x40 only as a labeled quick exploratory run.
- 🔴 **Boundary-condition card** — The text names patches `movingWall`, `fixedWalls`, and `referencePressure` (`ui/frontend/src/data/learnCases.ts:140`), but the current generator names patches `lid`, `wall1`, `wall2`, `bottom`, `frontAndBack` (`src/foam_agent_adapter.py:6835-6865`) and writes `pRefCell/pRefValue` in the `SIMPLE` block (`src/foam_agent_adapter.py:979-990`). The workflow also tells me to edit `0/U` with `movingWall` and `fixedWalls` (`ui/frontend/src/data/learnCases.ts:146`), which will not match the actual `blockMeshDict` if I copy the generator. Fix: publish one complete set of dictionaries, or at least keep patch names identical across prose, runbook, and generator.
- 🔴 **Observable extraction card** — This is the highest-risk narrative block. It says u_centerline is Ghia Table II and v_centerline is Table I, and says the case "also supports" v_centerline plus primary vortex as cross-validation (`ui/frontend/src/data/learnCases.ts:141`). The gold YAML says the opposite for u and explicitly warns that v_centerline and primary_vortex_location are not corrected and not exercised by the current audit comparator (`knowledge/gold_standards/lid_driven_cavity.yaml:26-32`, `knowledge/gold_standards/lid_driven_cavity.yaml:47-56`). As a novice, I would trust the page and write a false report claiming three validation channels exist. Fix: replace the card with an honest state: current automated comparison = u_centerline only; v_centerline and vortex center are planned but blocked by gold/reference cleanup.
- 🟠 **Benchmark lineage** — The lineage block is useful and names Burggraf, Ghia, Botella-Peyret, and Shankar-Deshpande (`ui/frontend/src/data/learnCases.ts:157-164`). The missing novice bridge is "which line of my report uses which source?" I need a short mapping: Ghia for centerline values, Burggraf for historical context, Botella-Peyret for high-accuracy follow-up, Shankar-Deshpande for broader flow physics. Fix: add "how to cite in your report" bullets.
- 🔴 **Reproducibility and troubleshooting blocks** — The component renders the workflow as if it is "complete command/file sequence" (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:528-550`), but the LDC data only gives high-level editing instructions and omits complete file contents (`ui/frontend/src/data/learnCases.ts:142-149`). Troubleshooting is helpful for residuals, mesh, overshoot, pressure reference, and missing secondary vortices (`ui/frontend/src/data/learnCases.ts:150-155`), but it depends on observables the current page does not teach me to compute, especially primary vortex location and secondary-vortex visibility. Fix: include a copy-paste minimal case or a generated reference bundle that contains the exact dictionaries.

## Step 2 findings — Reproducibility walk
- **Step 1: 准备 case 骨架** — Command shown: `foamNewCase ldc_re100 && cd ldc_re100` (`ui/frontend/src/data/learnCases.ts:143`). I cannot trust this as a pasteable command. My OpenFOAM tutorial experience used copying `$FOAM_TUTORIALS/incompressible/icoFoam/cavity/cavity`; I do not know whether `foamNewCase` exists in my OpenFOAM version. The detail says "从 ... icoFoam/cavity 拷贝模板" but the command does not copy anything. Also the page has already switched to `simpleFoam`, so starting from an `icoFoam` tutorial needs explicit changes to `controlDict`, `fvSchemes`, and `fvSolution`. Fix: give a tested command sequence, e.g. copy the tutorial or download the harness reference case, then list every file to change.
- **Step 2: 写 blockMeshDict** — Command shown: `$EDITOR system/blockMeshDict` (`ui/frontend/src/data/learnCases.ts:144`). I cannot execute the step without inventing the full dictionary. The line says vertices `(0 0 0)-(1 1 0.1)`, cells `(40 40 1)`, patches `movingWall/fixedWalls/frontAndBack`. The actual generator uses `convertToMeters 0.1`, cells `(129 129 1)`, and patches `lid/wall1/wall2/bottom/frontAndBack` (`src/foam_agent_adapter.py:6812-6865`). Fix: embed the full `blockMeshDict` or link the exact generated file.
- **Step 3: 生成网格** — Command shown: `blockMesh` (`ui/frontend/src/data/learnCases.ts:145`). This is executable, but the expected artifact is wrong for the audit path: it says `nCells=1600`, while the actual generated mesh is 129x129x1 = 16641 cells (`src/foam_agent_adapter.py:6826-6829`, `knowledge/gold_standards/lid_driven_cavity.yaml:98-103`). It also says "用 checkMesh" but does not include the command or pass criteria. Fix: add `checkMesh | tee log.checkMesh`, expected `Mesh OK`, expected cell count, and known acceptable warnings.
- **Step 4: 设置 BC 与 Re** — No command is shown (`ui/frontend/src/data/learnCases.ts:146`). This is not runnable for a novice because I need the full `0/U`, `0/p`, and properties file. It says `constant/transportProperties` and `nu=0.01`; the actual generator writes `constant/physicalProperties` and computes `nu=0.001` because the physical length is 0.1 m (`src/foam_agent_adapter.py:756-782`). It says `movingWall` and `fixedWalls`; actual `0/U` uses `lid`, `wall1`, `wall2`, `bottom`, `frontAndBack` (`src/foam_agent_adapter.py:1009-1059`). Fix: put the full dictionaries on the page or in the reference zip.
- **Step 5: 启动 solver** — Command shown: `simpleFoam > log.simpleFoam &` (`ui/frontend/src/data/learnCases.ts:147`). This is close to executable, but it omits `foamLog log.simpleFoam`, how to stop/wait for completion, and how to know the final time was written. The detail says `writeInterval=500`; current generator writes `writeInterval 2000` in `controlDict` for fields (`src/foam_agent_adapter.py:844-850`) and Phase 7 sample function writes every 500 iterations (`src/foam_agent_adapter.py:701-727`). Fix: distinguish field write interval from sample write interval and give expected files after iteration 1000/2000.
- **Step 6: 抽取 u-centerline** — Command shown: `postProcess -func sampleDict` (`ui/frontend/src/data/learnCases.ts:148`). I do not know if this is correct for the current OpenFOAM setup because the generator embeds a `functions { sample { type sets; ... } }` block in `controlDict` (`src/foam_agent_adapter.py:698-741`) and separately writes `system/sampleDict` for gold-anchored point sampling (`src/foam_agent_adapter.py:1116-1132`). The run artifacts show files like `sample/1000/uCenterline.xy`, not `postProcessing/sampleDict/<t>/line_x05_U.xy` as the page claims. Fix: specify the exact expected path, columns, coordinate normalization, and the command that generated it for this repo's OpenFOAM version.

## Step 3 findings — Gold comparison, 5-dimension check

Current strict Compare tab count: **1 visible scalar dimension**. `CompareTab` renders one gold stat block, one measured stat block, and one tolerance band from `ValidationReport.gold_standard`/`measurement` (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:897-945`). It does not render a profile, v-centerline, vortex location, stream-function extrema, kinetic energy, wall shear, or a grid-convergence plot. Across the whole `/learn` page, the Story tab embeds a scientific report iframe for LDC (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:1840-1919`) and the Mesh tab shows a convergence slider (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:1073-1230`), so the student can see more evidence outside Compare. But for independent gold-comparison dimensions, I count **at most 2 across the full page**: u_centerline profile and a grid-convergence trend on one u_centerline sample. Derived metrics such as L2, L∞, RMS, max deviation, and pass count are useful statistics, but they are not independent physics observables.

| Candidate dimension | Computed by current extractor/comparator? | Shown in Compare tab today? | In gold YAML? | If missing, what specifically to add |
|---|---|---|---|---|
| 1. `u_centerline` profile, x=0.5 vertical line | **Yes.** LDC generation writes uCenterline sampling (`src/foam_agent_adapter.py:1116-1132`), runtime parsing populates `u_centerline` (`src/foam_agent_adapter.py:7678-7712`), fallback extraction also populates `u_centerline` (`src/foam_agent_adapter.py:7908-7980`), and comparison-report metrics load only the u_centerline doc (`ui/backend/services/comparison_report.py:134-154`, `ui/backend/services/comparison_report.py:427-431`). | **Not as a profile in Compare tab.** Compare tab shows scalar stat/tolerance only (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:897-945`). The profile appears in the Story embedded scientific report/iframe, not the Compare tab (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:1848-1917`). | **Yes.** `quantity: u_centerline`, 17 y/u values, tolerance, source (`knowledge/gold_standards/lid_driven_cavity.yaml:59-97`). | Move or duplicate profile overlay and pointwise-deviation bar into Compare tab. Also label the scalar anchor's y coordinate clearly because the current stat block hides that the measurement may be a selected point. |
| 2. `v_centerline` profile, y=0.5 horizontal line | **No live sim extractor.** The LDC branches only populate `u_centerline` (`src/foam_agent_adapter.py:7342-7350`, `src/foam_agent_adapter.py:7457-7462`, `src/foam_agent_adapter.py:7678-7712`). Static public literature plot includes v, but that is not solver extraction (`ui/frontend/src/data/flowFields.ts:19-31`). | **No.** Compare tab's generic stat/tolerance UI has no second profile slot (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:897-945`). | **Present but explicitly suspect.** Header says v_centerline appears mislabeled and uncorrected (`knowledge/gold_standards/lid_driven_cavity.yaml:26-32`); the v document exists (`knowledge/gold_standards/lid_driven_cavity.yaml:115-145`). | First repair gold axis labels to x-based Table II; then add horizontal sample line to `foam_agent_adapter.py`, parser key `v_centerline`, comparator support, render `profile_v_centerline.png`, and Compare tab panel. |
| 3. `primary_vortex_location` `(x_c,y_c)` | **No.** Current extraction never computes stream function or vortex center; the only LDC numerical extractor is u_centerline (`src/foam_agent_adapter.py:7908-7980`). Rendered streamlines are visual only. | **No.** No Compare tab UI for two-coordinate error or vortex marker (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:897-945`). | **Present but wrong/incomplete.** YAML stores x=0.5000 and y=0.7650 while the contract says Ghia Re=100 is `(0.6172,0.7344)` and current block is not exercised (`knowledge/gold_standards/lid_driven_cavity.yaml:50-56`, `knowledge/gold_standards/lid_driven_cavity.yaml:163-176`). | Correct gold values first; add post-processing for vortex center, ideally from stream-function extrema or velocity stagnation/recirculation center; render marker on contour and a numeric error card. |
| 4. Stream-function `ψ_min` primary-vortex strength | **No.** The real render uses velocity streamplot, not computed ψ (`scripts/render_case_report.py:511-520`). The public `stream_function.png` is an ansatz calibrated to profiles, not solver ψ (`ui/frontend/src/data/flowFields.ts:27-30`). | **No.** | **No.** YAML has no ψ observable (`knowledge/gold_standards/lid_driven_cavity.yaml:59-189`). | Add a stream-function post-process step or compute ψ by integrating velocity on the structured grid; add literature value/source and tolerance; render `psi_min` card and contour marker. |
| 5. Secondary-vortex `ψ_max` / corner vortices | **No.** No extractor key for secondary vortices. The contour may visually show a tiny lower-right curl, but no numeric locations/strengths are captured. | **No.** | **No.** | Add lower-corner vortex detection after ψ computation; store separate gold values for bottom-left/bottom-right where literature supports them; render callouts. |
| 6. Integrated kinetic energy over cavity | **No.** Current LDC report computes profile statistics only (`ui/backend/services/comparison_report.py:187-211`) and does not integrate the velocity field. | **No.** | **No.** | Add field integral over VTK cells in `scripts/render_case_report.py` or backend report service; add reference value or use it as grid-convergence diagnostic if no canonical Ghia value exists. |
| 7. Wall shear stress along moving lid | **No for LDC.** There is wall-shear infrastructure for plane-channel work, but not LDC. LDC function objects sample U/p and residuals only (`src/foam_agent_adapter.py:698-741`). | **No.** | **No.** | Add OpenFOAM `wallShearStress` function object for LDC, extract lid-wall distribution, define reference/provenance, and render a lid shear plot. |
| 8. Grid convergence study, L2 error vs 1/N / GCI | **Partially.** Mesh tab reads `mesh_20/40/80/160` validation reports (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:1073-1230`). Backend comparison report loads synthetic mesh rows from fixtures and computes GCI (`ui/backend/services/comparison_report.py:242-287`, `ui/backend/services/comparison_report.py:434-438`). But this is still one u-profile/scalar convergence path, not a second physical observable. | **No in Compare tab.** It is Mesh tab / embedded report, not Compare tab. | **No explicit convergence sequence.** Gold YAML has mesh_info cells but no grid-study gold (`knowledge/gold_standards/lid_driven_cavity.yaml:98-103`). | Put grid convergence on Compare tab as one dimension, label whether fixtures are synthetic or real, and compute L2(profile) vs 1/N rather than only one sample point. |

Two additional hazards came out of this table. First, the Compare tab's scalar anchor is itself confusing: `validation_report.py` turns multi-document YAML into `observables[]` by taking the first non-zero scalar in `reference_values` (`ui/backend/services/validation_report.py:107-154`), while `audit_real_run_measurement.yaml` reports a selected failed point `u_centerline[y=0.3750]`. So the scalar Compare tab can compare a measured point at one y against a gold scalar chosen from another y unless the backend has already aligned it. Second, the page text claims v-centerline and primary-vortex support, but the gold contract explicitly says those are not corrected and not exercised (`knowledge/gold_standards/lid_driven_cavity.yaml:26-32`, `knowledge/gold_standards/lid_driven_cavity.yaml:47-56`). For a novice report, this is not just shallow; it can create false claims.

## Step 4 findings — Contour / geometry consistency
- **Specified path mismatch** — The prompt path `reports/lid_driven_cavity/phase5_renders/` does not exist in this checkout. The actual LDC render path is `reports/phase5_renders/lid_driven_cavity/20260421T082340Z/`, with a manifest at `reports/phase5_renders/lid_driven_cavity/runs/audit_real_run.json`. This should become a finding because a student or Claude patcher following old paths will think renders are missing.
- **`reports/phase5_renders/lid_driven_cavity/20260421T082340Z/contour_u_magnitude.png`** — This is the strongest current visual. It is recognizable as a square cavity: axes are `x/L`, `y/L`, aspect is visually square, and streamlines show a clockwise primary vortex near the upper-right half. The script enforces equal aspect and labels axes/colorbar (`scripts/render_case_report.py:511-520`). However, it lacks the novice callouts the user asked for: no "moving lid y=1, U=1 →" annotation, no primary vortex center label, no bottom-corner secondary-vortex labels, no Ghia side-by-side figure, no scale note explaining the colorbar is `|U|/U_lid`. It supports the mental image better than earlier visual-only assets, but it is not yet a teaching figure.
- **`reports/phase5_renders/lid_driven_cavity/20260421T082340Z/profile_u_centerline.png`** — This is not a geometry/contour image; it is a useful profile comparison. It shows a blue sim curve and red Ghia markers, but the citation text at the bottom is truncated and a novice may not know why y=0.75/0.8125 deviations matter. It should be moved into Compare tab with explanatory labels.
- **`reports/phase5_renders/lid_driven_cavity/20260421T082340Z/pointwise_deviation.png`** — This is the best existing error visualization. It shows 11/17 pass and red bars around y=0.6875-0.8125. It answers "where is the disagreement?" but not "why does disagreement cluster there?" It also does not connect to geometry, so the student cannot map failed y positions to top shear layer / vortex-center movement without extra explanation.
- **`reports/phase5_renders/lid_driven_cavity/20260421T082340Z/residuals.png`** — Useful convergence evidence, not a contour. It has Ux/Uy/p residuals and log scale. It supports "solver converged" but not "flow physics correct." The Run tab correctly warns that convergence does not equal correctness (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:1450-1454`).
- **`ui/frontend/public/flow-fields/lid_driven_cavity/centerline_profiles.png`** — This is a literature/reference plot, not solver output. It includes both u and v Ghia curves, which is helpful, but the current harness only compares u. Because the Story page renders it under "Literature reference" (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:358-387`) and its caption names v as well (`ui/frontend/src/data/flowFields.ts:21-24`), I would assume v validation is available. That assumption is false today.
- **`ui/frontend/public/flow-fields/lid_driven_cavity/stream_function.png`** — This looks like a square cavity and has top arrows, but its provenance says it is a tensor-product ansatz calibrated to Ghia centerline profiles, not full DNS (`ui/frontend/src/data/flowFields.ts:27-30`). Visually, it is more symmetric and centered than the actual OpenFOAM contour. As a student, I see two different "pictures" of the same flow: the synthetic ψ plot center looks around the cavity middle, while the actual contour's primary vortex is upper-right. The page needs to explicitly label this as pedagogical only and put the real contour next to it.
- **Does the rendering look alien?** For LDC specifically, the current real contour does not look alien; it looks like a plausible square cavity. The issue is not gross geometry mismatch. The issue is insufficient annotation plus inconsistent visual hierarchy: synthetic literature/ansatz figures and real solver figures are separated, not side-by-side, and only the real contour reveals the main vortex shape described in `physics_intuition_zh` (`ui/frontend/src/data/learnCases.ts:136`).

## Step 5 — Novice report stub
Using only the current `/learn/lid_driven_cavity` page, I would write the following report stub:

The lid-driven cavity is a canonical two-dimensional incompressible Navier-Stokes benchmark. The geometry is a square cavity. The top wall moves to the right with nondimensional velocity \(U=1\), while the other three walls are no-slip. The Reynolds number is defined as \(Re=UL/\nu\), and the page focuses on Re=100. At this Reynolds number the flow is laminar and steady, so the page recommends `simpleFoam`, a steady SIMPLE solver, instead of a transient PISO/PIMPLE run. The main physical picture is that the moving lid drags the upper fluid layer to the right, viscosity transfers momentum downward, and the closed side and bottom walls force the flow to recirculate. This creates a large clockwise primary vortex and small counter-rotating secondary vortices near the lower corners.

For validation, the page uses Ghia, Ghia & Shin 1982 as the gold standard. The primary observable is the horizontal velocity along the vertical centerline \(x=0.5\). The simulated \(u/U_{lid}\) profile should be compared to Ghia's tabulated values at multiple \(y/L\) locations. The page says the comparison is important because matching this profile is the first proof that the solver, mesh, boundary conditions, and extraction path are reasonable. The real OpenFOAM report shows a contour with streamlines, a centerline profile plot, a pointwise deviation plot, and a residual convergence plot. The residuals decay to around \(10^{-5}\), so the numerical iteration appears converged. The profile plot indicates that many Ghia points are close, but the pointwise deviation plot reports only 11/17 points inside the 5% tolerance. Therefore I would describe the current result as partially matching Ghia, not a clean validation pass.

【I don't know】The page does not give me a complete `blockMeshDict`, `0/U`, `0/p`, `fvSchemes`, or `fvSolution`, so I cannot reproduce the run from the page without inventing files. 【I don't know】The workflow says 40x40 cells and `nu=0.01`, but the report figure says 129x129 and the generator uses `convertToMeters 0.1` with `nu=0.001`; I do not know which setup belongs in my course report. 【I don't know】The observable card says v-centerline and primary-vortex location are supported, but the current Compare tab only gives me one scalar tolerance band and the gold YAML warns that v-centerline and vortex center are not corrected. I cannot honestly claim five validation dimensions. 【Page doesn't say】I do not know how to compute the stream function, vortex strength, secondary vortex positions, integrated kinetic energy, or wall shear stress. 【Page doesn't say】I do not know how to cite the synthetic stream-function image versus the real OpenFOAM contour, or why the synthetic picture looks more symmetric than the real contour. Because of these gaps, the report can describe the physical story and one u-centerline comparison, but it cannot yet be a full reproducibility/validation report.

## Step 6 — Recommended patches
- 🔴 **Make one authoritative LDC run path.** In `ui/frontend/src/data/learnCases.ts:142-149`, replace the six-step workflow with the exact current audit path: 129x129 mesh, patch names `lid/wall1/wall2/bottom/frontAndBack`, `constant/physicalProperties`, `nu=0.001` for the 0.1 m generator path, `simpleFoam`, and expected `sample/<iter>/uCenterline.xy`. If the desired student path is dimensionless 1x1 with `nu=0.01`, label it explicitly as a separate tutorial path and do not mix it with audit output.
- 🔴 **Embed or link complete dictionaries.** At `ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:528-550`, add an expandable "copy exact files" block for `blockMeshDict`, `0/U`, `0/p`, `physicalProperties`, `controlDict`, `fvSchemes`, `fvSolution`, and `sampleDict`. The current runbook says it is complete, but it is not complete enough to run.
- 🔴 **Fix LDC solver/mesh/prose drift.** Align `solver_setup_zh`, `mesh_strategy_zh`, and `boundary_conditions_zh` (`ui/frontend/src/data/learnCases.ts:138-140`) with the generator: actual cells `(129 129 1)` (`src/foam_agent_adapter.py:6826-6829`), actual relaxation factors (`src/foam_agent_adapter.py:992-1001`), actual scheme (`src/foam_agent_adapter.py:907-912`), actual patch names (`src/foam_agent_adapter.py:6835-6865`). If 40x40 remains, call it a quick run and state it should not be used for the final validation report.
- 🔴 **Stop claiming unsupported comparison channels.** Rewrite `observable_extraction_zh` (`ui/frontend/src/data/learnCases.ts:141`) so it does not say v_centerline and primary_vortex_location are supported today. The gold YAML explicitly says those blocks are suspect and not exercised (`knowledge/gold_standards/lid_driven_cavity.yaml:26-32`, `knowledge/gold_standards/lid_driven_cavity.yaml:47-56`).
- 🔴 **Raise Compare tab from scalar to multi-dimensional evidence.** Modify `CompareTab` (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:792-947`) to show at least the LDC u profile, pointwise deviation bars, and grid-convergence trend directly, not only the scalar tolerance band. Then add v-centerline and vortex center once gold/extractor are fixed. The Story iframe is not enough; the tab named Compare must be where comparison lives.
- 🔴 **Repair or quarantine v_centerline and vortex gold.** In `knowledge/gold_standards/lid_driven_cavity.yaml:115-176`, either correct v_centerline axis/value provenance and primary vortex `(x_c,y_c)`, or add UI-visible "not currently validated" labels. Do not let the page teach them as valid while the contract says they are not.
- 🟠 **Add five-dimension pilot schema for LDC before rolling to the other cases.** Minimal LDC target: u_centerline, v_centerline, primary vortex center, ψ_min or vortex strength, and grid convergence L2(profile) vs 1/N. Secondary vortices, kinetic energy, and lid wall shear can be phase-2 additions if literature references are not ready.
- 🟠 **Annotate the real contour.** Extend `scripts/render_case_report.py:511-520` to add a lid arrow at y=1, primary-vortex marker, bottom-corner labels where visible, square-cavity outline, and a note that colors are `|U|/U_lid`. The current plot is scientifically recognizable but under-labeled for a novice.
- 🟠 **Put synthetic and real visuals side-by-side.** The public stream-function asset is pedagogical ansatz, not solver output (`ui/frontend/src/data/flowFields.ts:27-30`). The Story tab should show it beside the real OpenFOAM contour with labels "pedagogical reconstruction" vs "actual audit_real_run output" so students do not confuse them.
- 🟠 **Add a novice glossary.** Under the physical intuition block (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:405-415`), add definitions for SIMPLE, URF, residual, stream function, vortex center, GCI, and tolerance band. Keep this short, but make every term used in `learnCases.ts:136-141` discoverable without Googling.
- 🟠 **Fix the scalar/profile split-brain.** `ValidationReport` already has `profile_verdict` fields (`ui/backend/schemas/validation.py:253-276`) and backend computes 11/17 profile status (`ui/backend/services/validation_report.py:929-997`), but Compare tab does not surface that split. Add "scalar anchor verdict" vs "profile verdict" labels so the student does not read a single PASS/FAIL as the whole profile truth.
- 🟡 **Make report-writing guidance explicit.** After benchmark lineage (`ui/frontend/src/data/learnCases.ts:157-164`), add "what to write in your report" bullets: geometry/BC, solver/mesh, convergence evidence, u-profile comparison, known limitations, and not-yet-supported dimensions.
- 🟡 **Normalize paths in docs and UI copy.** The prompt path `reports/lid_driven_cavity/phase5_renders/` is stale; actual files live under `reports/phase5_renders/lid_driven_cavity/...`. Use the actual path in future review prompts and any downloadable reproduction README.

## Overall pilot verdict
- Current reading value for a novice trying to reproduce and write a course report: **4/10**.
- It would rise to **8/10** if these are fixed: one authoritative copy-paste run path; complete OpenFOAM dictionaries; honest u-only comparison state; Compare tab with at least five independent validation dimensions; corrected v/vortex gold; annotated contour with real-vs-synthetic distinction; a glossary for SIMPLE/URF/GCI/stream function; and report-writing prompts that tell the student exactly what claims are supported.
- Is "just fix LDC and roll to 9 others" feasible? **Partly, but the gap is structural.** LDC is the easiest case, and it already exposes the structural problem: the content layer, gold YAML, backend comparator, static reference images, live render path, and Compare tab are not a single coherent learning contract. A per-case content patch will help, but rolling to the other 9 should not happen until the LDC pilot establishes a reusable pattern: supported-observable table, copy-paste runbook, five-dimension comparison model, render annotation standard, and explicit "not implemented yet" labels. Otherwise the other cases will inherit the same one-scalar validation problem with more complicated physics.
