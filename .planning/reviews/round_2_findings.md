# DEC-V61-046 Round 2 — Codex Consolidated Verdict

**Overall verdict**: CHANGES_REQUIRED
**Blocker count**: 1

## Role 1 — 商业立项 demo 评审专家
**Verdict**: APPROVE_WITH_COMMENTS
- R1-M1 [verify]: addressed in `6911611` — the `/learn` hero is now buyer-facing and bilingual, leading with “Prove a CFD result is physically trustworthy...” plus Chinese support copy for team leads/auditors (`ui/frontend/src/pages/learn/LearnHomePage.tsx:45-56`).
- R1-M2 [verify]: addressed in `6911611` — `PhysicsContractPanel` is rendered first inside `StoryTab`, above the explanatory sections, so the reader sees the contract before the downstream PASS/HAZARD/FAIL surface (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:331-339`).
- R1-M3 [verify]: addressed in `6911611` — the home-page CTA strip now includes GitHub, `/pro`, and a mailto inquiry path (`ui/frontend/src/pages/learn/LearnHomePage.tsx:82-117`).
- R1-M4 [verify]: addressed in `6911611` — the three-strip differentiation band now names real-solver evidence, literature-backed comparator, and signed audit package explicitly (`ui/frontend/src/pages/learn/LearnHomePage.tsx:57-79`).
- R1-M5 [verify]: addressed in `6911611` — the placeholder-alert nav items are gone; the top nav only exposes real `/learn` and `/pro` routes (`ui/frontend/src/components/learn/LearnLayout.tsx:22-47`).
- R1-N1 [verify]: addressed in `6911611` — the left-rail tagline is now buyer-readable and the internal Path-B provenance moved to the footer (`ui/frontend/src/components/Layout.tsx:43-49`, `ui/frontend/src/components/Layout.tsx:90-93`).
New findings:
- R1-N2 — still present at `61140c4`: the browser title remains `CFD Harness — Validation Report`, so the first browser-level impression is still the old internal/report surface (`ui/frontend/index.html:7`).
- R1-N3 — still present at `61140c4`: the Advanced-tab audit-package bridge drops `case`/`run` context even though the header CTA preserves it; the two paths are inconsistent (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:234-237`, `ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:1372-1374`).

## Role 2 — CFD 仿真专家工程师
**Verdict**: CHANGES_REQUIRED
- R2-M1 [verify]: addressed in `6c53986` — LDC precondition #4 and #5 are now split into two explicit-false claims (`v_centerline`, `primary_vortex_location`) with evidence that explains why each is not currently physics-valid (`knowledge/gold_standards/lid_driven_cavity.yaml:47-52`).
- R2-M2 [verify]: partially addressed in `6c53986` — the contract narrative removed the old hard `<2%` plateau claim, but the anchor block still stores `6.26` under a Le/Moin/Kim 1997 source that the same file describes as `6.28`, and the observable description still repeats the `<2%` drift language (`knowledge/gold_standards/backward_facing_step.yaml:16-18`, `knowledge/gold_standards/backward_facing_step.yaml:35-42`).
- R2-M3 [verify]: addressed in `6911611` — plane-channel mesh labels are now honest cell counts (`20³/40³/80³/160³`) and the in-file comment explicitly says the old `WR-LES` / `DNS` labels overstated the solver path (`ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:98-112`).
- R2-M4 [verify]: addressed in `6c53986` — impinging_jet now frames A4 as the observable symptom, names the root cause as composite, and defers root-cause audit to Phase 9 rather than pretending one knob fix exists (`knowledge/gold_standards/impinging_jet.yaml:15-18`, `knowledge/gold_standards/impinging_jet.yaml:27-28`).
- R2-M5 [deferral]: accept — the current long-prefix taxonomy is overloaded but not silently drifting today: the DEC log records the follow-up scope, and `_normalize_contract_class()` plus its new prefix test pin the current dashboard behavior (`.planning/decisions/2026-04-22_demo_first_convergence_dec046_multi_persona_codex_review.md:89-95`, `tests/test_report_engine/test_contract_dashboard_report.py:152-186`, `src/report_engine/contract_dashboard.py:160-174`, `src/report_engine/contract_dashboard.py:855-859`).
- R2-M6 [verify]: addressed in `6911611` — the audit-package positioning is now described as “inspired by” DO-178C / V&V40 / NQA-1 rather than equivalent to them (`ui/frontend/src/pages/learn/LearnHomePage.tsx:73-77`).
- R2-M7 [verify]: addressed in `fa7d96d` — the dashboard now points `gold_file` at canonical YAMLs while keeping legacy `report_case_id` only for fixture lookup, and the case-detail / Story-tab path reads canonical gold by whitelist `case_id` via `_load_gold_standard(case_id)` (`src/report_engine/contract_dashboard.py:57-68`, `src/report_engine/contract_dashboard.py:740-742`, `ui/backend/services/validation_report.py:80-95`, `ui/backend/services/validation_report.py:826-854`).
- R2-M8 [deferral]: accept — the TFP fallback hazard is already surfaced as an explicit `partial` precondition with a follow-up note; promoting it to a first-class `AuditConcern` is real adapter/plumbing work and can reasonably sit in a dedicated follow-up DEC (`.planning/decisions/2026-04-22_demo_first_convergence_dec046_multi_persona_codex_review.md:96-101`, `knowledge/gold_standards/turbulent_flat_plate.yaml:27-30`).
- R2-N1 [verify]: addressed in `6c53986` — LDC mesh wording now says “Ghia high-resolution reference shoulder” instead of “DNS-quality shoulder” (`knowledge/gold_standards/lid_driven_cavity.yaml:53-55`).
New findings:
- None beyond the unresolved `R2-M2` partial-fix above.

## Role 3 — Senior Code Reviewer
**Verdict**: CHANGES_REQUIRED
- R3-M1 [verify]: addressed in `fa7d96d` — dashboard specs now read canonical gold files, and the parity test locks that invariant so dashboard + live `/api/cases` do not drift again (`src/report_engine/contract_dashboard.py:57-68`, `src/report_engine/contract_dashboard.py:740-742`, `tests/test_report_engine/test_contract_dashboard_report.py:123-145`).
- R3-M2 [verify]: addressed in `6c53986` — BFS wording and adapter code now consistently point to `kEpsilon`, not `kOmegaSST` (`knowledge/gold_standards/backward_facing_step.yaml:23-25`, `src/foam_agent_adapter.py:1564-1569`).
- R3-N1 [verify]: addressed in `fa7d96d` — `_normalize_contract_class()` prefix handling is now explicitly pinned by test (`tests/test_report_engine/test_contract_dashboard_report.py:148-186`, `src/report_engine/contract_dashboard.py:855-859`).
- R3-N2 [verify]: addressed in `93e84cf` — `_precondition_marker()` now covers booleans, string booleans, partial labels, ints, and unknown types fail-visible instead of silently laundering them (`ui/backend/routes/case_export.py:126-153`, `ui/backend/tests/test_case_export.py:122-152`).
- R3-N3 [verify]: addressed in `93e84cf` — export now has a live regression test that checks `[✗]` rendering for LDC’s explicit-false preconditions (`ui/backend/tests/test_case_export.py:155-174`).
- R3-N4 [verify]: addressed in `93e84cf` — the default-run docstrings now correctly describe `audit_real_run` as the first-choice path (`ui/backend/services/validation_report.py:290-301`, `ui/backend/services/validation_report.py:764-769`).
- R3-N5 [verify]: addressed in `93e84cf` — frontend README now points to port `5180` and documents overrides (`ui/frontend/README.md:19-23`).
New findings:
- R3-B1 — still not addressed at `61140c4`: the live `ValidationReport` path converts `satisfied_by_current_adapter: partial` to `True`. `_make_preconditions()` does a `bool(...)` cast, the backend schema still declares `satisfied: bool`, the TS mirror still declares `satisfied: boolean`, and the Story-tab panel is therefore working from a lossy model even though export is now honest (`ui/backend/services/validation_report.py:423-438`, `ui/backend/schemas/validation.py:138-144`, `ui/frontend/src/types/validation.ts:52-57`, `ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:460-482`). Direct runtime probe on 2026-04-23 confirmed `build_validation_report("backward_facing_step")` returns `True, True, True, False` for the four BFS preconditions, so the buyer-facing Story tab still paints three documented `partial` clauses as green checks.

## Regressions / new issues (any persona)
- No broad build/test regression surfaced in targeted verification: `.venv/bin/pytest -q ui/backend/tests/test_case_export.py tests/test_report_engine/test_contract_dashboard_report.py` passed `23/23`, and `pnpm typecheck` plus `pnpm build` both succeeded on 2026-04-23.
- The main residual correctness gap is that round-2 fully fixed export honesty, but not the live `ValidationReport`/Story-tab path (`ui/backend/services/validation_report.py:423-438`, `ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:460-482`).
- The main residual physics gap is the BFS anchor/source inconsistency that batch 1 only partially cleaned up (`knowledge/gold_standards/backward_facing_step.yaml:16-18`, `knowledge/gold_standards/backward_facing_step.yaml:35-42`).

## Deferral judgment (single paragraph each)
- R2-M5: Accept. The current `contract_status` strings are overloaded, but they are not silently drifting today: the DEC log records the follow-up scope, `CONTRACT_CLASS_ORDER` keeps `SATISFIED` ahead of `COMPATIBLE`, and the new unit test pins the prefix behavior directly. Splitting this into `base_verdict / scope_qualifier / hazard_flags` touches backend schema, frontend types, dashboard, export, and all 10 YAMLs, which is legitimate dedicated-DEC territory rather than a remediation side quest (`.planning/decisions/2026-04-22_demo_first_convergence_dec046_multi_persona_codex_review.md:89-95`, `src/report_engine/contract_dashboard.py:160-174`, `src/report_engine/contract_dashboard.py:855-859`, `tests/test_report_engine/test_contract_dashboard_report.py:152-186`).
- R2-M8: Accept. The Spalding-fallback risk is already visible to a reviewer in the current gold contract: TFP precondition #4 explicitly marks the fallback branch as `partial` and explains the follow-up audit intent. Promoting that internal flag into a first-class `AuditConcern` still requires real adapter/emitter/attestor plumbing, so deferring it is acceptable as long as the current contract text stays explicit (`.planning/decisions/2026-04-22_demo_first_convergence_dec046_multi_persona_codex_review.md:96-101`, `knowledge/gold_standards/turbulent_flat_plate.yaml:27-30`).

## If CHANGES_REQUIRED: round-3 directive
1. `ui/backend/services/validation_report.py:423-438`, `ui/backend/schemas/validation.py:138-144`, `ui/frontend/src/types/validation.ts:52-57`, `ui/frontend/src/pages/learn/LearnCaseDetailPage.tsx:463-482` — stop `bool(...)`-casting `satisfied_by_current_adapter`; carry the raw tri-state (`true` / `false` / `partial` or an explicit enum) through the API and render it directly in the Story-tab `PhysicsContractPanel`. Add a regression test at the service/API layer that proves BFS preconditions remain partial in the live `ValidationReport`, not just in export.
2. `knowledge/gold_standards/backward_facing_step.yaml:16-18`, `knowledge/gold_standards/backward_facing_step.yaml:35-42`, `src/report_engine/contract_dashboard.py:79-82`, `src/report_engine/contract_dashboard.py:823-826` — make the BFS anchor internally consistent. Either change the reference value/copy to a clean Le/Moin/Kim `6.28` with regime-level wording only, or explicitly rename/cite `6.26` as a blended engineering anchor. Do not leave the current state where the same file cites `6.28`, stores `6.26`, and still repeats the disavowed `<2%` claim.
