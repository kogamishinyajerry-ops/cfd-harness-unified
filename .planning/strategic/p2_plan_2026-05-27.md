# Blueprint v4 · P2 phase plan — pre-flight signals + ruleset distillation

> Status: **DRAFT (awaiting sponsor approval before building)** · 2026-05-27 · author: cfd-chief-engineer (L2)
> Parent: DEC-V61-207 (Blueprint v4) · follows DEC-V61-209/210 (P1 V&V loop complete)
> SSOT for P2 scope. Increments land as sub-DECs / spike-class commits per v2.3.

## Goal (one line)

Catch bad/likely-wrong CFD setups **before** the expensive solve (pre-flight signals), and
distill the death-mode corpus into the **offline** advisor ruleset so the workbench advises
LLM-free at runtime (Law-3). Both strictly **advisory-only** (DEC-V61-130/132 untouchable).

## Why now (motivating evidence)

P1's flat-plate arc (DEC-V61-209) burned ~8 solve cycles on exactly the failure classes
pre-flight is meant to pre-empt: Re mismatch (2e6 vs NASA 5e6 → 15–17% error), y+~120 vs
canonical y+~1, freestream Tu 600× off canonical. A deterministic pre-flight signal would
have flagged all three before the first Docker solve. P2 turns this session's hard-won lessons
into reusable, offline guardrails.

## Current state (verified — Explore map 2026-05-27)

**Pre-flight** (`ui/backend/services/preflight.py`, route `GET /api/cases/{id}/preflight`,
schema `ui/backend/schemas/preflight.py`): 5 categories LANDED — physics, schema, mesh
(Richardson GCI), gold_standard, adapter. Advisory-only confirmed (zero writes; `require_loopback`).
`PreflightCategory` is a free `str`, so new categories are additive. `build_preflight(case_id)`
aggregates; frontend stage-4 close needs ≥5 categories (already met → a 6th is safe/additive).

**V9 ruleset** (SSOT JSON `ui/frontend/src/data/v9_advisor_rules.json` v9.0.0 + Python binding
`ui/backend/services/v9_advisor/rules.py`): 8 rules R1–R8, **all POST-run** (residual
oscillation, max-iters, crash exit, gold-delta, force drift, slow conv, plateau, healthy).
Pure predicates over `RunArtifactSlice`, zero I/O → genuinely LLM-offline. **No unit tests for
individual predicate correctness** (only integration tests). No PRE-run rules yet.

**Distillation source** (`docs/openfoam_corpus/industrial_solver_findings_v_series.md`, 1366
lines, 85+ V-rows, status taxonomy closed/partial/open/playbook). ~9 distilled into v9; ~40
"closed/playbook" rows are ready to distill; rest are open domain gaps (VOF/LES/compressible).

**Advisory surfacing** (`/api/cases/{id}/ai-review` + `/ai-diagnose`, `advisor_stack.py`, 11
geometry/solver advisors): V130/V132 contracts enforced (read-only, LLM-offline, V-row
traceability). Provably no mutation. v9 ruleset is orthogonal (convergence) to advisor_stack
(geometry).

## Workstreams & increments (each = sub-DEC or spike; `passes` = E2E-verified, not just COMPLETED)

### W1 — pre-flight validity signals (fill the High-blocker gaps)
| # | Increment | passes-criterion | size |
|---|---|---|---|
| W1.1 | ~~**flow_coherence**: Reynolds coherence + y+ estimate~~ **BLOCKED — re-scoped (see below)** | — | — |
| **W1.0 (NEW prereq)** | Add sourced machine-readable `canonical_conditions {Re,U,L,ν}` to gold standards (each value cited; truth-chain) — the independent reference W1.1 needs | gold standards expose canonical Re/U/L/ν as data; each carries a citation; schema-valid | sub-DEC |
| W1.1′ (after W1.0) | **flow_coherence**: Re-coherence = live case `U·L/ν` vs **gold `canonical_conditions.Re`** (NOT self-derived); topology gate (`role==inlet` required, excludes lid/internal); laminar→Blasius `Cf=0.664/√Re_x`, turbulent→Prandtl-Schlichting, else skip; y+ scoped OUT until first-cell-height wired (skip-with-honest-evidence) | fires a real fail on a param-drifted case vs canonical; correctly SKIPs lid_driven_cavity/internal; no turbulent Cf on laminar; advisory-only; pytest green | sub-DEC |

> **W1.1 adversarial-review finding (ultrawork workflow `wf_c2bb500a`, 2026-05-27 — verified against repo):** the
> originally-planned W1.1 is **circular theater**. `knowledge/workbench_basics/*.yaml` author `derived[Re]`
> *from the same* `U·L/ν` (e.g. flat plate `formula: "Re = U·L/ν = 1.0×1.0/2e-5"`), so `U·L/ν` vs `derived[Re]`
> ≈ 1.0 by construction — it validates self-consistency, not physical correctness, and cannot catch the
> DEC-V61-209 param-drift it was meant to catch. Three independent adversaries returned BLOCK/CONCERNS/BLOCK.
> Additional verified defects: (a) `lid_driven_cavity` (lid `role: moving_wall`, no inlet) false-PASSes via the
> "any fixedValue vector" U-fallback — fix = gate on `role==inlet`; (b) all shippable cases are LAMINAR
> (`laminar: true`, Blasius), so the planned turbulent Prandtl-Schlichting Cf is an 87% math error — fix =
> regime-gate on the laminar flag; (c) y+ check can never fire (no readable first-cell-height) so it must
> skip-with-honest-evidence, not imply coverage; (d) the 2× Re band conflated a Cf tolerance with a Re
> tolerance, and the y+~1 anchor + a V3 citation were unsourced (truth-chain violations). **Resolution: split
> into W1.0 (wire the independent canonical reference, sourced) then W1.1′ (the real, non-circular check).**
| W1.2 | **bc_consistency** category: reference-pressure patch presence (incompressible needs a pressure reference) + inlet/outlet role cross-check | passes on a case missing pRef → fail row; clean case → pass | sub-DEC |
| W1.3 | mesh-quality advisory: surface checkMesh synopsis (max skewness / non-orthogonality / AR) as advisory rows | passes: AR≈502 flat-plate surfaces as advise-row, not silent | sub-DEC |

### W2 — ruleset distillation (corpus → offline rules)
| # | Increment | passes-criterion | size |
|---|---|---|---|
| W2.0 | **per-predicate unit tests** for existing R1–R9 (close the test gap before adding rules) | each predicate has a true-positive + true-negative test; pytest green | spike — **LANDED** commit `afeeb4a` (5 boundary negatives R1/R5/R6/R7/R8; R2/R3/R4/R9 already had them) |
| W2.0.5 | **Truth-chain invariant tests** on the ruleset (every provenance carries a recognized citation marker — V-row id / textbook author / §-section / DOI; commentary length banded; severity enum-locked) — prevents future "uncited threshold" rules (the exact W1.1 truth-chain failure mode) | new `TestRulesetTruthChain` green on all 9 landed rules; would fail on a synthetic rule with `provenance: "industry consensus"` | spike — **this commit** |
| **W2.0.6 (NEW prereq)** | **Slice extension for regionally-aware patterns**: extend `RunArtifactSlice` (Python + TS dataclass parity) with structured post-run fields — e.g. `developed_region_gold_delta` (regional vs scalar `max_abs_pct`), `integrated_drag_pct` (the NASA-convention metric DEC-209 introduced), `reference_comparison_band_summary` (n-near-LE-deviations etc.). Update audit-side glue to populate. The cross-language fixtures get a new shape. | extended slice; both bindings produce identical matches against a fixture exercising the new fields | sub-DEC (medium — touches data contract on both sides) |
| W2.1 (after W2.0.6) | Distill **DEC-209 post-run lessons** into rules using the extended slice: a "known-deviation pattern" rule that distinguishes "global Cf delta high but developed-region clean + integrated metric passes" (= known formulation discrepancy → informational) from "delta high everywhere" (= real solver bug → warn) | new rules fire correctly on the DEC-209 fixture and similar synthetic shapes; parity holds | sub-DEC |
| W2.2 | Distill **N "closed/playbook" V-rows** into rules (batch; candidates: V4 GAMG agglomeration failure, V37 plateau-triage extension to multiple residual fields, V41 thermophysical-bound violation) — selected to be NON-redundant with existing R1–R9 (skip V3/V6/V7: already in R9) | each rule has provenance + true/false fixtures; no LLM import; not subsumed by an existing rule | sub-DEC ×N |

> **W2 meta-finding (P2 W2.0/W2.0.5 recon, 2026-05-27):** the post-run DEC-209 lessons cannot be cleanly
> distilled into v9 rules with the current `RunArtifactSlice` dataclass — it carries only a scalar
> `gold_delta.max_abs_pct`, not the regional structure (developed-region vs near-LE, integrated drag, station
> Cf) that the DEC-209 verification gate produced. Trying to write a rule on "scalar max delta is high but
> the run was actually OK" is theater-by-omission: the rule has no way to tell the two patterns apart. The
> existing R9 (`RESIDUAL_DIVERGENCE_V9_R9`) already cites V3+V6+V7 (kΩSST ω blowup / mass-flow zero-IC /
> compressible ρ negative) for the divergence class, so adding an R10-ω-specific rule would co-fire with R9
> and add advice-overlap without new catch-coverage. **The honest path is W2.0.6 (slice extension) before
> W2.1.** This mirrors W1's structural pattern: both P2 workstreams need their data contracts widened
> before substantive rule/check distillation can land with integrity. Recorded so a future session doesn't
> re-discover this the hard way.

## Recommended FIRST increment → **W1.1 (flow_coherence pre-flight)**

Highest value (directly operationalizes this session's lessons + fills the two "High" blocker
gaps), lowest risk (additive category, advisory-only, no route change, no schema break — just a
new `str` category + rows + tests). Deterministic and LLM-offline by construction.

**Acceptance (W1.1 passes):**
1. `build_preflight("flat_plate_rans_sst")` emits a `flow_coherence` category with a Reynolds-
   coherence row (pass: Re/L=5e6 matches gold) and a y+ row (advise: y+~1.3 in-band).
2. A Re-mismatched fixture (e.g. ν=1.5e-5 → Re/L=2e6) yields a **fail** row whose `evidence`
   names the measured vs expected Re and `consequence` names the wasted-solve risk.
3. Zero writes (advisory-only); `require_loopback` intact; four-question gate answered in the
   commit body.
4. New pytest covers both the pass and fail fixture; full sweep green (incl `cfdtrust_tests/`).
5. Correctness-critical shared code → Codex review (cap=3) before commit.

## Guardrails (persist at L2 — non-negotiable)
- **Advisory-only**: pre-flight + ruleset NEVER mutate a case dir (DEC-V61-130/132). Enforced by
  existing tests + four-question gate on every increment.
- **LLM-offline**: every new predicate/check is a pure function; `test_stack_zero_llm_imports`
  must stay green.
- **No silent gate-tuning**: pre-flight signals are advisory, not pass/fail gates on the V&V
  tolerance — they never touch the DEC-209 reference tolerances.
- **Truth-chain**: every distilled rule cites its V-row provenance; no fabricated rules.

## Open question for sponsor
Approve W1.1 as the first build, or reprioritize (e.g. lead with W2.0 test-debt closure, or a
specific V-row batch)? On approval I build W1.1 end-to-end (code + tests + Codex review + commit).
