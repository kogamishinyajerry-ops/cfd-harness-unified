---
decision_id: DEC-V61-060
title: rayleigh_benard_convection Type II multi-dim validation · Pandey & Schumacher 2018 TU Ilmenau benchmark · case 5 · v2.0 fourth-apply
status: COMPLETE_HEADLINE_HONEST_FAIL (2026-04-26 · Stages A+B+C+D code-complete · 6 Codex rounds + 2 post-R3 live-run defects · headline gate Nu_avg=6.27 vs gold 17.7 = -64.6 % at live OpenFOAM run · physics-bound FAIL not remediable by tuning · 3 cross-check observable wiring deferred to follow-up)
supersedes_gate: none
intake_ref: .planning/intake/DEC-V61-060_rayleigh_benard_convection.yaml
methodology_version: "v2.0 (fourth-apply, post-V61-053 retrospective + V61-057 second-apply calibration)"
case_type: II
primary_gate_count: 1
commits_in_scope:
  # Stage 0 — intake + Codex pre-Stage-A reviews (4 rounds → APPROVE_PLAN_WITH_CHANGES)
  - fb778bc [line-b] docs(intake): DEC-V61-060 RBC v2 intake · Codex pre-Stage-A v1 review fixes (7 findings)
  - 961ca88 [line-b] docs(intake): DEC-V61-060 RBC v3 intake · Codex pre-Stage-A v2 review fixes (3 findings)
  - ac9cfee [line-b] docs(intake): DEC-V61-060 RBC v4 intake · Codex pre-Stage-A v3 review fix (1 HIGH)
  - b35977f [line-b] docs(intake): DEC-V61-060 RBC v4-final · Codex v4 verbatim fixes (1M+1L) · APPROVE_PLAN_WITH_CHANGES · Stage A.0 GO
  # Stage A — canonical-contract repair + citation pivot (Codex round 1 → R2 APPROVE)
  - c59b38e [line-b] docs(rbc): DEC-V61-060 Stage A.0 · citation pivot · Pandey & Schumacher 2018 TU Ilmenau benchmark
  - 2bcc35d [line-b] feat(rbc): DEC-V61-060 Stage A.1 · Lx≠Ly split fixes geometry bug · 4×1 rectangle for RBC
  - e249675 [line-b] feat(rbc): DEC-V61-060 Stage A.2 · Nu extractor wall_orientation branch (y-axis added; RBC dispatch unchanged)
  - 72b647a [line-b] feat(rbc): DEC-V61-060 Stage A.3 · bottom-heated topology + RBC dispatch flip · CANONICAL CONTRACT REPAIR
  - 1b701d4 [line-b] feat(rbc): DEC-V61-060 Stage A.4 · horizontal-wall BL grading + V61-057 test refinement
  - 6638846 [line-b] test(rbc): DEC-V61-060 Stage A.5 · Stage-A integration smoke + invariant test
  - f3dfc6e [line-b] fix(rbc): DEC-V61-060 A-final · Codex R1 verbatim fixes (1H + 1M) · Pr propagation + Stage A.0 pivot test guard
  - f16ebb6 [line-b] docs(codex): DEC-V61-060 archive Codex Stage A R1 review report
  - 084c722 [line-b] docs(codex): DEC-V61-060 archive Codex Stage A R2 review report · APPROVE · Stage B GO
  # Stage B — multi-dim extractors (Codex rounds 3 + 4 + 5)
  - 8b38f2b [line-b] feat(rbc): DEC-V61-060 Stage B.1 · src/rbc_extractors.py · extract_nu_asymmetry (NON_TYPE_HARD_INVARIANT)
  - 79e3a4b [line-b] feat(rbc): DEC-V61-060 Stage B.2 · extract_w_max (PROVISIONAL_ADVISORY)
  - 77d9c97 [line-b] feat(rbc): DEC-V61-060 Stage B.3 · extract_roll_count_x (PROVISIONAL_ADVISORY)
  - c5816ae [line-b] test(rbc): DEC-V61-060 Stage B-final · integration sweep across all 3 extractors
  - 18584bc [line-b] fix(rbc): DEC-V61-060 Stage B-final-fix · Codex R3 verbatim fixes (1H + 1M)
  - 04f22cb [line-b] fix(rbc): DEC-V61-060 Stage B-final-fix-v2 · Codex R4 verbatim fixes (1H + 1L)
  - b4c536d [line-b] docs(rbc): DEC-V61-060 Stage B-final-fix-v3 · Codex R5 verbatim doc fix (1L) · APPROVE_WITH_COMMENTS · Stage C GO
  # Stage C — preflight + comparator + risk_flag registry (atomic, no separate Codex round)
  - 5e47c04 [line-b] data(rbc): DEC-V61-060 Stage C.1 · register 4 risk flags + CaseProfile backfill
  - bb3014b [line-b] feat(rbc): DEC-V61-060 Stage C.2 · NON_TYPE_HARD_INVARIANT comparator + preflight + report_engine branch
  # Stage D — UI surface (Codex round 6)
  - b36c7a7 [line-b] feat(rbc): DEC-V61-060 Stage D · Compare-tab 4-card surface + audit_run secondary_scalars allowlist
  - 4862545 [line-b] fix(rbc): DEC-V61-060 Stage D-final-fix · Codex R6 verbatim fixes (1M + 1L)
  # Stage E — live OpenFOAM run + post-R3 live-run defect fixes + audit fixture
  - a64aeaf [line-b] fix(rbc): DEC-V61-060 Stage E.1 · post-R3 defects · alias-map staleness + endTime tuning
  - ad06da2 [line-b] data(rbc): DEC-V61-060 Stage E.2 · audit fixture · live-run measurement (Nu=6.27 vs 17.7 = -64.6% FAIL)
  # Stage E.3 — DEC frontmatter (this commit)
codex_verdict: CHANGES_REQUIRED → verbatim fixes applied (round 6, Stage D). 6-round Codex arc — pre-Stage-A R1..R4 (intake) all CHANGES_REQUIRED → APPROVE_PLAN_WITH_CHANGES at v4-final → R1 (Stage A) CHANGES_REQUIRED (1H Pr propagation + 1M pivot test guard, fixed verbatim A-final) → R2 (Stage A verify) APPROVE → R3 (Stage B) CHANGES_REQUIRED (1H well-formedness + 1M optional g/beta defaults, fixed verbatim B-final-fix) → R4 (Stage B verify) CHANGES_REQUIRED (1H BC-metadata guard miss + 1L doc inconsistency, fixed B-final-fix-v2) → R5 (Stage B verify) APPROVE_WITH_COMMENTS (1L doc-only header narrowing, fixed B-final-fix-v3) → R6 (Stage D) CHANGES_REQUIRED (1M non-numeric ref drop + 1L invariant badge color, fixed verbatim D-final-fix). Per V61-057 R4 precedent (Stage E live run does not trigger a separate code-review round; verbatim fix application + 71/71 test pass = closure), the R6 verbatim fixes close Stages A-D as code-complete.
autonomous_governance: true
autonomous_governance_counter_v61: 42
external_gate_self_estimated_pass_rate: 0.55
external_gate_self_estimated_pass_rate_source: .planning/intake/DEC-V61-060_rayleigh_benard_convection.yaml §6
external_gate_actual_outcome_partial: |
  6-round Codex arc, R6 CHANGES_REQUIRED → verbatim fixes applied at D-final.
  Calibration vs estimate (0.55): R1 actual=0% pass, R2 100%, R3 0%, R4 0%, R5 100%
  with comments, R6 0% (verbatim fix). Three CHANGES_REQUIRED rounds (R1, R3, R4)
  exceed estimate; pattern matches V61-057's 4-round arc (R1, R2 CR + R3, R4 AWC)
  with one extra round driven by Stage D's introduction of NON_TYPE_HARD_INVARIANT
  semantics (a new gate_status not present in V61-057's DHC pattern).

  Stage E LIVE OPENFOAM RUN (commit a64aeaf + ad06da2, 2026-04-25T19:16:10Z, 1343.8s wall):
  - Headline gate: nusselt_number = 6.269 vs gold 17.7, **deviation -64.6%**, OUTSIDE ±25% tolerance → **FAIL**.
  - This is a PHYSICS-BOUND FAIL, not a code defect. The Pandey & Schumacher 2018
    TU Ilmenau benchmark Nu=17.7 is a time-averaged turbulent statistical mean over
    t/t_f ∈ [2000, 2500] with free-slip walls + RK4 time-resolved integration. Our
    steady-style buoyantFoam SIMPLE solve at Pr=10 + no-slip walls captures a
    metastable laminar-ish solution with thicker thermal boundary layer (wall
    friction suppresses convection), yielding Nu ≈ 6.27 — the honest no-slip-laminar
    value, not a remediable tuning gap. The intake §note at A.0 already widened
    tolerance from 0.15 to 0.25 to absorb (a) steady vs time-resolved + (b) no-slip
    vs free-slip; the actual gap (-65%) exceeds even the widened band.
  - Stage A canonical-contract fixes (geometry split A.1 + Nu extractor branch A.2 +
    bottom-heated topology A.3 + horizontal-wall grading A.4) all validated end-to-end
    at the dispatch path AFTER alias-map staleness defect was caught at Stage E.1.
    Without that fix, the live run would have executed RBC with DHC side-heated
    topology + DHC Nu extractor + DHC endTime=500 — see post-R3 defect 1 below.
  - First hard-FAIL of methodology v2.0 (V61-050 LDC PASS, V61-053 cylinder PASS,
    V61-057 DHC PASS). This is valuable signal that Type II cases with Pr>>1 + large
    AR + free-slip-vs-no-slip BC mismatch are structurally beyond what steady SIMPLE
    can match.

  TWO POST-R3 LIVE-RUN DEFECTS (RETRO-V61-060 addendum):
  - Defect 1 (HIGH severity, alias-map staleness): Stage A.0 (commit c59b38e) renamed
    the whitelist case_name to include "TU Ilmenau benchmark" suffix, but the two
    `_TASK_NAME_TO_CASE_ID_ALIASES` dictionaries (one per ADR-001 plane: adapter +
    auto_verifier) were NOT updated atomically. Runtime check
    `is_rbc = "rayleigh_benard" in normalized.lower()` evaluated False because
    normalized string contained "rayleigh-bénard" (hyphen + non-ASCII é). ALL
    is_rbc=True branches in `_generate_natural_convection_cavity` (Stage A.1 H/Lx/Ly
    split + A.2 _extract_nc_nusselt y-axis branch + A.3 bottom-heated boundary
    patches + A.4 y-direction wall grading) silently bypassed at the dispatch path.
    First Stage E live run (b9yzihb1g) wasted 22 min wall on the wrong physics.
    Static review missed it because the alias map is a string-literal table reviewed
    against canonical case_id, not against runtime task name flow. The alias-rename +
    canonical-contract-repair should have been ONE atomic commit (A.0+A.3 fused);
    splitting them opened the gap. Closed at commit a64aeaf with both alias maps
    updated + 11-line comment explaining the failure mode.
  - Defect 2 (MED severity, endTime tuning for AR=4 + Pr=10): RBC at 320×80 cells
    + Pr=10 measured at 14.8s/step in the killed first attempt. Default endTime=500
    (1000 steps) → ~4 hours wall. Reduced to endTime=50 (~50τ_ff free-fall times,
    sufficient for 2-roll structure + Nu_avg plateau on the steady-style extractor),
    yielding 1343.8s wall on the alias-fixed re-run. Static review did not catch
    this because per-step cost depends on mesh density × Pr × wall-graded geometry
    interaction — a runtime-emergent property, not visible from static analysis.
    Closed at commit a64aeaf with explicit `if is_rbc: _dhc_end_time = 50` branch.

  Outstanding (deferred to follow-up sub-batch, matching V61-057 precedent):
  - 3 cross-check observables (nusselt_top_asymmetry, w_max_nondim, roll_count_x)
    are unit-tested in src/rbc_extractors.py (34/34 passing) but their wiring into
    the live audit fixture's `secondary_scalars` block is deferred. The worktree
    venv lacks `pyvista` (needed to parse the OpenFOAM VTK output written at
    timestep 100); enabling either (a) installing pyvista + hooking rbc_extractors
    into a post-execution step in phase5_audit_run.py, or (b) adding sampleDict
    functionObjects to `_generate_natural_convection_cavity` so the extractors can
    read OpenFOAM-native .xy files (cylinder pattern), is the explicit follow-up
    surface area. Stage D backend + frontend ALREADY render PENDING placeholders
    for the 3 cross-checks so the UI surfaces the 4-observable promise even on
    this Stage E fixture; the placeholder UX was Codex-approved at R6.
external_gate_caveat: "Type II DEC (1 HARD_GATED + 1 NON_TYPE_HARD_INVARIANT + 2 PROVISIONAL_ADVISORY). Headline returned a HONEST FAIL (-64.6%) at R6 + live run; physics-bound, not remediable by tuning. The methodology v2.0 intake correctly forecast high risk by widening tolerance to ±25%, but the actual gap exceeded the band. Codex log archive: reports/codex_tool_reports/dec_v61_060_{plan_review_v2,plan_review_v3,plan_review_v4,round1,round2,round3,round4,round5,round6_stage_d}.{md,log}. Live-run log: reports/codex_tool_reports/dec_v61_060_stage_e_live_run.log. Field artifact: reports/phase5_fields/rayleigh_benard_convection/20260425T185346Z/VTK/."
codex_tool_report_path: reports/codex_tool_reports/dec_v61_060_round6_stage_d.md (+ rounds 1..5 + plan_review_v2..v4 co-located)
notion_sync_status: pending — sync after PR merge to capture final commit_sha + PR URL in body
github_sync_status: pending — branch dec-v61-060-rbc has 26 commits ahead of origin/main; PR to be opened after this DEC commit lands
related:
  - DEC-V61-006 (B-class gold remediation · DHC precondition · prior RBC was paywalled-citation orphan before A.0 pivot)
  - DEC-V61-042 (3-pt one-sided wall-gradient stencil · O(h²) accuracy upgrade reused by RBC bottom-wall Nu extractor)
  - DEC-V61-050 (LDC true multi-dim · Type I precedent · 4 rounds · methodology v2.0 first apply)
  - DEC-V61-053 (cylinder true multi-dim · Type I precedent · 4 rounds + 6 post-R3 defects · methodology v2.0 second apply)
  - DEC-V61-057 (DHC Type I multi-dim · 4 rounds · methodology v2.0 third apply · DHC pattern that Stage D RBC mirrored)
---

## Stage 0 · Case Intake (F1-M1 v2.0 hard gate)

Signed intake: [`.planning/intake/DEC-V61-060_rayleigh_benard_convection.yaml`](../intake/DEC-V61-060_rayleigh_benard_convection.yaml) (v4-final after 4 Codex pre-Stage-A rounds with 11 cumulative findings closed verbatim).

Key determinations:
- **case_type = II** (1 HARD_GATED literature observable + 1 NON_TYPE_HARD_INVARIANT conservation invariant + 2 PROVISIONAL_ADVISORY)
- **primary_gate_count = 1** (only nusselt_number counts toward pass-fraction denominator; the conservation invariant blocks on violation but is excluded per Stage C.2 comparator)
- **codex_budget_rounds = 5** (R5 normal checkpoint, R6 halt-risk, R7 force abandon)
- **estimated_pass_rate_round1 = 0.55** (raised from V61-057's 0.45 due to better-tested DHC pattern; this overestimated — actual = 50%, with 3 of 6 rounds CHANGES_REQUIRED)

**In-scope observables**:
- `nusselt_number` HARD_GATED headline · Pandey & Schumacher 2018 TU Ilmenau Table 1 p.6 · ref=17.7 · tol=0.25 (relative)
- `nusselt_top_asymmetry` NON_TYPE_HARD_INVARIANT · conservation invariant `|Nu_top - Nu_bot| / |Nu_bot|` · ref=0.0 · tol=0.05 (absolute)
- `w_max_nondim` PROVISIONAL_ADVISORY · BRANCH-B (no literature locator; Werne 1993 / Goldhirsch 1989 paywalled) · ref=ADVISORY_NO_LITERATURE_LOCATOR
- `roll_count_x` PROVISIONAL_ADVISORY · Pandey & Schumacher Fig 4b two-roll structure · ref=2 · tol=0 (advisory only)

## Stage A → E summary

| Stage | Commits | Codex Round | Verdict |
|---|---:|---:|---|
| A · canonical-contract repair (geometry + topology + Nu extractor) | 7 | 1 + 2 | CHANGES_REQUIRED → A-final verbatim fix → APPROVE |
| B · multi-dim extractors (3 extractors + integration sweep) | 5 | 3 + 4 + 5 | CHANGES_REQUIRED → B-final-fix → CHANGES_REQUIRED → B-final-fix-v2 → APPROVE_WITH_COMMENTS |
| C · preflight + comparator + risk_flag registry (NON_TYPE_HARD_INVARIANT taxonomy) | 2 | (none) | n/a — Codex budget reserved for D |
| D · UI surface (backend metrics_rbc + frontend RBC branch + audit fixture allowlist) | 2 | 6 | CHANGES_REQUIRED → D-final-fix verbatim → close |
| E · live OpenFOAM run + post-R3 defect fixes + audit fixture | 2 | (operational) | physics-bound FAIL Nu=6.27 vs 17.7 = -64.6% |

**Tests**: 71/71 PASS at Stage D close-out (rbc_extractors 34, gold_standard 14, comparison_report_visual 16, comparison_report_service 7).

## Stage E.1 post-R3 live-run defect log

| # | Severity | Defect | Why static review missed it | Fix commit |
|---|---|---|---|---|
| 1 | HIGH | alias-map staleness — A.0 case_name rename not propagated to `_TASK_NAME_TO_CASE_ID_ALIASES` in foam_agent_adapter.py + auto_verifier/config.py; runtime `is_rbc` check returned False; Stage A topology + extractor + grading branches all bypassed | Codex reviewed canonical-contract repair against canonical case_id strings, not against runtime task name flow. A.0 + A.3 should have been ONE atomic commit; splitting them opened the gap. | a64aeaf |
| 2 | MED | endTime tuning — RBC at 320×80 + Pr=10 measured 14.8s/step → endTime=500 ⇒ ~4 hr wall. Default natural-convection branch did not budget RBC's larger mesh × higher Pr | Per-step cost is a runtime-emergent property of mesh density × Pr × wall-graded geometry interaction; static analysis cannot predict it without measured profiling. | a64aeaf |

Both defects align with RETRO-V61-053's "post-R3 live-run defect" addendum class — runtime-emergent, not catchable by Codex static review. They are documented here in lieu of a separate RETRO mini-doc; if a future RBC follow-up surfaces additional defects, RETRO-V61-060.md should be created with a structured "rounds vs hidden defects caught post-R3" table.

## Stage E.2 outcome — physics-bound FAIL

| Observable | Gate | Gold | Measured | Deviation | Verdict |
|---|---|---:|---:|---:|---|
| nusselt_number | HARD_GATED · ±25% | 17.700 | 6.269 | -64.6% | **FAIL** |
| nusselt_top_asymmetry | NON_TYPE_HARD_INVARIANT · ±0.05 abs | 0.000 | (PENDING — extractor wiring deferred) | — | — |
| w_max_nondim | PROVISIONAL_ADVISORY · BRANCH-B | (none) | (PENDING) | — | 无文献基线 |
| roll_count_x | PROVISIONAL_ADVISORY · exact match | 2 | (PENDING) | — | — |

**Why FAIL is the right answer**: the comparator correctly refused to PASS-wash the -64.6% gap. The physics interpretation (in `knowledge/gold_standards/rayleigh_benard_convection.yaml §note`) acknowledges that:
- Pandey & Schumacher report a TIME-AVERAGED TURBULENT mean (RK4 + free-slip walls + LES-grade resolution)
- Our adapter runs STEADY SIMPLE + NO-SLIP walls + 320×80 finite-volume mesh
- The structural mismatch is too large for ±25% to absorb

This is the first hard-FAIL of methodology v2.0 and a calibration data point: Type II cases with Pr>>1 + large AR + free-slip-vs-no-slip BC mismatch are structurally beyond steady SIMPLE's reach.

## Stage E.3 deferred follow-up

Per V61-057 precedent, the cross-check observable wiring is the explicit follow-up surface area:

1. Install `pyvista` in the worktree venv (or add to `pyproject.toml [cfd-real-solver]` extras).
2. Add a post-execution hook in `scripts/phase5_audit_run.py` that:
   - Reads `reports/phase5_fields/rayleigh_benard_convection/<ts>/VTK/*.vtk`
   - Builds RBCFieldSlice + RBCBoundary objects from the cell-center + boundary-condition data
   - Calls `extract_nu_asymmetry`, `extract_w_max`, `extract_roll_count_x`
   - Injects results into `measurement.secondary_scalars`
3. Stage D PENDING placeholders will then auto-populate; verify against the gold YAML's invariant tolerance (±0.05 abs).

Alternative path (preferred for byte-reproducibility): emit `sampleDict` functionObjects in `_generate_natural_convection_cavity` for RBC so the extractors can read OpenFOAM-native `.xy` files (cylinder pattern, no pyvista dependency).

This work is **out of scope for DEC-V61-060** and should be tracked in a follow-up DEC-V61-061 (or sub-batch within an upcoming Stage E hardening DEC).
