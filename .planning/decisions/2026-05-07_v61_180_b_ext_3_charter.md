---
decision_id: DEC-V61-180
title: B-ext-3 charter · F10 fix (setup-bc → polyMesh/boundary patch-name contract) — drive verdict pass ≥ 1/3 on DeepSeek cells (no budget cap)
status: Accepted
parent_dec: V61-179
phase: B-extend-3
notion_sync_status: pending
---

# DEC-V61-180 · B-Ext-3 Charter

## Status

**Accepted 2026-05-07** — Per B-ext-2 close (DEC-V61-179) and user
explicit budget authorization ("no DeepSeek limit, push project
forward regardless of cost"). Continues B-arc → B-extend → B-ext-2
arc; F10 is now positively identified as workbench-side BC contract
bug.

## Scope

Workbench-side fix + integration test + iterative R7 live runs
until charter target met.

| Sub-DEC | Capability |
|---|---|
| DEC-V61-181 | B-ext-3.1 investigate setup-bc → polyMesh/boundary patch-name divergence |
| DEC-V61-182 | B-ext-3.2 fix F10: align 0/<field>/boundaryField keys with mesh patch names |
| DEC-V61-183 | B-ext-3.3 Steps 1-5 E2E integration test (one per charter case) |
| DEC-V61-184 | B-ext-3.4 R7 live re-run; iterate until verdict pass ≥ 1/3 |
| DEC-V61-185 | B-ext-3.5 close + Chinese delta summary |

## Charter goal

**verdict pass ≥ 1/3** on R7 (or R8+ if needed). B-ext-2 close
demonstrated 0/3 /solve POST 200 due to F10 (BC patch-name
mismatch). Once F10 fixed and an E2E integration test guards the
contract, R7 with persona Step 6 prompts + max_steps=80 +
max_input_tokens=3M should close the gap.

## F10 reproduction (from DEC-V61-178)

```bash
# After successful Steps 1-4 sequence on any charter case:
curl -sX POST -H "Content-Type: application/json" -d '{}' \
  http://localhost:8000/api/import/<case_id>/solve
# → 502 with body:
# {"detail":{"failing_check":"solver_diverged","detail":
#  "simpleFoam exited with code 1; ... log.icoFoam ..."}}
```

`log.icoFoam` shows:

```
--> FOAM FATAL IO ERROR:
Cannot find patchField entry for patch0
file: /tmp/.../<case>/0/p/boundaryField from line 6 to line 7.
```

setup-bc writes `patch0/patch1/...` keys; mesh
`polyMesh/boundary` (after F7 patch-split) has semantic names
(`inlet/outlet/wall`).

## Out of scope (B-ext-4 candidates)

- 6 deferred charter cells (Anthropic + gpt-5.4) — still gated on
  ANTHROPIC_API_KEY + CODEX_RELAY_API_KEY
- Workbench-side `/solve-sync` mode
- Workbench taxonomy unification (`/api/cases/...` vs `/api/import/...`)
- Mesh-quality / actions catalogue payload trimming

## Termination criteria

- **HARD success**: any single live run submits a passing verdict
  (verdict.passed=True, observed within reference ± tolerance)
- **HARD success**: verdict pass ≥ 1/3 across the 3 DeepSeek cells
- **HARD bound**: if R7 + R8 + R9 don't yield ≥1/3 even with F10
  fixed, escalate to user with new diagnosis (F11+ workbench-side
  layer)

User pre-authorization (re-affirmed 2026-05-07): no budget cap.
Iterate as needed.

## V130 / V132 contract

No new mutating routes. F10 fix is a contract correction inside
existing setup-bc service (read polyMesh/boundary, emit matching
keys) OR an internal remap layer in the solver call. V130
advisory-only persona contract: zero impact (engineer still drives).
V132 MUTATING_ROUTES + KNOWN_MUTATION_FUNCTIONS: zero impact (no
new routes added). Contract test 21/21 must continue passing.

## Verification

- `pytest tests/dogfood/ ui/backend/tests/` 全绿 after each commit
- New B-ext-3.3 integration test must pass on all 3 charter cases
- R7 (and any subsequent R-iterations) artifacts under
  `.planning/dogfood/runs/live_2026_05_07_r7*/`
- Aggregator-generated DOGFOOD_REPORT_LIVE_R7.md with severity table
- Final close summary documents which R-iteration first crossed
  verdict pass = 1 (and which cell)

## Counter

B-ext-3 increment: minimum +5 (charter +1, B-ext-3.1+3.2+3.3+3.4+3.5
+5). Could be more if B-ext-3.4 splits into multiple R-iteration
commits.

## References

- DEC-V61-179 · B-ext-2 close
- `.planning/dogfood/B_EXT_2_CLOSE_SUMMARY_ZH.md` §F10
- `.planning/dogfood/runs/live_2026_05_07_r6/` — F10 trajectory
