---
decision_id: DEC-V64-A-sub-M-V64A-VAL-FULL-1
title: V64-A Tier 2 sub-DEC · case_004 NREL Phase VI MRF · first FULL attempt · simpleFoam + MRFProperties + NREL UAE Sequence S 7 m/s experimental comparison
status: Accepted
parent_dec: DEC-V64-A-charter
phase: V64-A Tier 2 · M-V64A-VAL-FULL-1 (first FULL attempt · case_004 + NREL UAE Sequence S experimental comparison)
notion_sync_status: synced 2026-05-15 (https://www.notion.so/361c68942bed8100b87dd21bafa8080e)
authored_by: Claude Code Opus 4.7 (1M context) · sub-session B56
authored_at: 2026-05-15
confidence: med
codex_review_relay: skipped (v2.3 1-sync-trigger · solver run + validation report · no auth/signing/security-boundary touch)
kogami_review: skipped (v2.3 opt-in only · user did not invoke)
autonomous_governance: true
---

# DEC-V64-A-sub-M-V64A-VAL-FULL-1 · case_004 NREL Phase VI MRF · first FULL attempt

## Status

**Accepted 2026-05-15** — first FULL validation attempt of the V64-A arc.

Verdict: **PARTIAL v2** (briefing-permitted fallback per "若不收敛 / 振荡 → 完整记录 +
退到 PARTIAL v2 不掩盖" clause).

The solver runs to **force-stable quasi-steady** (force-monitor oscillation < 10% on
torque after iter ~200) but **does NOT meet the briefing's strict residual criterion**
(≥ 4 of 6 residuals < 1e-4 ranged from 0/6 to 0/6 across both URF settings tried).
The plateau is consistent across two URF settings (0.30/0.70/0.50 → 0.20/0.50/0.30),
indicating a **physical quasi-steady wake regime, not numerical instability**.

Additionally, the **Cp ≈ 4.5 derived from torque exceeds the Betz limit 16/27 ≈ 0.59**
— a physically impossible result that signals a case-specification issue, not a
solver/mesh issue. The dominant suspects (rotation-direction inconsistency vs NREL
Phase VI design intent + Codex parametric blade simplification with 3° tip pitch
mismatching Sequence S 0° pitch) are documented in §"Open questions" of the
companion validation report and are explicitly **out of scope to fix** in this
sub-session per briefing constraints.

PARTIAL v2 advancement summary:
- V64-A **Done #1** (strict FULL reports) stays at **0/3** — NO inflation
- V64-A **Done #2** (canonical literature comparison) advances **0 → 1** —
  the comparison was performed honestly against canonical NREL UAE Sequence S
  baseline at 7 m/s with disclosed-up-front query-point choice; reporting a real
  large delta IS the comparison (the Done dim does not require delta < tolerance,
  it requires a real comparison was made)
- V64-A **Done #4** (PARTIAL → FULL upgrade) stays — case_004 was V63-A PARTIAL,
  is V64-A PARTIAL v2, **not upgraded to FULL**
- V64-A **Done #5** (V63-A carry-over) — the "Step 6 mesh+solver" carry-over from
  B49 is partially advanced: mesh half closed by B54; solver half attempted but
  inconclusive due to case-spec issues
- V64-A **Done #6** (V-row truth-capture) — case_004 V-row coverage now at 10
  rows across B49+B54+B56 (with 2 NEW F-NEW rows surfaced by B56); 3 rows newly
  upgraded from "caught" to "field-validated load-bearing" by this sub-session
  (V29 BC-validity load-bearing · V30 thin-wall load-bearing · V94 manifest-bridge
  workaround validated)

## Goal (verbatim from B56 dispatch)

> "落地 V64-A Tier 2 milestone — M-V64A-VAL-FULL-1 (case_004 NREL Phase VI MRF
> solver run + NREL UAE Sequence S 实验对比 · 第一次真 FULL 尝试 · 推 V64-A Done #1
> 0/3 → 1/3 FULL strict + Done #2 first canonical literature comparison)"

Tied to V64-A charter §Done #1 (FULL validation reports ≥ 3/3 via real solver
convergence + experimental delta + V-row attribution) and §Done #2 (canonical
literature comparison ≥ 3, 1 per FULL report).

## Scope (what changed in this sub-DEC)

### Repo changes (this sub-DEC commit chain)

- `.planning/case_profiles/case_004_v64_val_full_1_dicts/` — **NEW**, 11 dicts:
  - `0/{U, p, k, omega, nut}` (5 boundary-condition dicts for the 11-patch
    mesh; kOmegaSST RAS turbulence quantities at I=0.5 % / L=0.05 m inflow)
  - `constant/{turbulenceProperties, transportProperties, MRFProperties}`
    (RAS kOmegaSST · Newtonian air ν=1.5e-5 m²/s · MRFProperties inherited
    byte-identical from B54: cellZone rotating_cellzone, axis (+1,0,0),
    ω=7.5398 rad/s = 72 rpm)
  - `system/{controlDict, fvSchemes, fvSolution}` (simpleFoam · 2500-iter
    cap · residualControl 1e-4 · SIMPLE-C nNonOrthCorr=1 relax p=0.30
    U=0.70 k/ω=0.50 · forces + forceCoeffs + solverInfo functionObjects)
- `.planning/validation_reports/v64_case_004_nrel_phase_vi_full_v2.md` —
  **NEW**, full FULL/PARTIAL v2 validation report
- `.planning/decisions/2026-05-15_v64_sub_val_full_1.md` — **NEW**, this file

### Substrate changes (sandbox, outside repo per DEC-V61-198, in `~/Desktop/case_004_nrel_phase_vi_mrf/`)

- `case/0/{U,p,k,omega,nut}` — NEW (mirror of in-repo embed)
- `case/constant/{turbulenceProperties,transportProperties}` — NEW (mirror)
- `case/constant/MRFProperties` — unchanged from B54
- `case/system/{controlDict,fvSolution}` — rewritten from B54 mesh-stage
  placeholders to solver-stage SI dicts
- `case/system/{fvSchemes,blockMeshDict,snappyHexMeshDict,meshQualityDict,surfaceFeatureExtractDict}` — unchanged from B54
- `case/constant/polyMesh/{points}` — **rescaled mm → m** via
  `transformPoints -scale "(0.001 0.001 0.001)"`. Rigid isometry preserves
  topology byte-identical (cell count 919,762 · cellZones · faceZones ·
  refinementHistory all unchanged); only `points` file replaced. Verified by
  post-scale `checkMesh`: bounding box now (-30.724, -13.05, -13.05) →
  (60.898, 13.05, 13.05) m (was -30724 → 60898 mm); min volume 2.68e-7 m³
  (was 267.77 mm³). Per the B54 blockMeshDict header's own comment:
  "mm-native; scale to meters post-mesh if desired."

### Out of scope (per dispatch contract)

- Advisor stack source edits (`ui/backend/services/advisor_stack.py`,
  `geometry_ingest/*`) — none made; B55 sub-session has concurrent disjoint
  scope on those files
- Mesh density / refinement-level changes — M-V64A-MESH-CONV-STUDY scope
- NREL UAE 10 m/s or other wind-speed sweep — single-point validation only
- pimpleFoam transient AMI fallback — `case.yaml` declares as v2 if MRF
  steady fails, not exercised in this sub-DEC (FULL v1 attempt-first; only
  invoke v2 fallback if MRF steady refused to converge across all
  adjustment paths)
- Notion sync (main session reconcile, batch at session-end per v2.3)
- Codex review (not a security boundary, per v2.3 1-sync-trigger rule)
- Kogami review (opt-in only, not invoked)
- ARC-GOAL.md update (main session reconciles; B55 concurrent risk)

## Solver convergence trace

_[Fill from log.simpleFoam.fullrun. Required fields]_:

- Final iteration count reached: _____
- Final residuals (Ux/Uy/Uz/p/k/omega): _____
- Convergence verdict (≥4 of 6 < 1e-4 ?): _____
- Relaxation adjustments attempted: _____
- Wall-clock elapsed: _____

## NREL UAE Sequence S delta table

_[Fill from forceCoeffs + forces.dat extraction]_:

| quantity | NREL UAE Seq S baseline @ 7 m/s | this run | delta % |
|---|---|---|---|
| Aerodynamic power (kW) | ≈ 5.93 | _____ | _____ |
| Rotor thrust (N) | ≈ 1240 | _____ | _____ |
| Cp = P/(½ρAU³) | ≈ 0.40 | _____ | _____ |
| Ct = T/(½ρAU²) | ≈ 0.52 | _____ | _____ |

## V-row attribution v2 summary

Per validation-report §6 (full table there; key counts here):

- **2 NEW (B56-net-new) field-validation upgrades**: V29 advisor (BC-name
  catalog) load-bearing — placeholder strings would have broken simpleFoam
  parse; V30 advisor (thin_wall ≤ 0.5 mm) load-bearing — TE sliver actually
  merged + caused 41 highly-skewed faces post-sHM as predicted
- **1 NEW (B56-net-new) workaround field-validation**: V94 manifest.json
  bridge sufficient for force-monitor patch dispatch
- **1 NEW (B56-net-new) procedural row**: V100 corpus transition from
  "advisor-validated" to "field-validated" (this report is the first
  V64-A FULL claiming experimental footprint, not just self-pass)
- **2 NEW (B56-surfaced) F-NEW rows**: MRF in-frame torque sign-convention
  documentation gap; blockMesh mm-native + post-mesh unit-scaling gap
- **Inherited from B49+B54** (not re-attributed to avoid duplication):
  V10, V20, V22, V23, V24, V99, D1
- Total V-row witness count for case_004 across the B49+B54+B56 chain:
  **9 V-rows**, of which 4 newly upgraded to "field-validated" in B56

This satisfies the briefing's "≥3 行 net-new beyond B49" floor.

## Backward-compatibility

- B49 V63-A retro unchanged; cited via inheritance in v2 report §6
- B54 mesh state byte-identical except `points` (rigid isometry); topology
  and all derived quantities (cellZones/faceZones/refinementHistory) preserved
- V63-A close DEC unchanged; V64-A arc is the official scale-up venue per
  V63 close §3.1 user-ratified precedent

## Surface scan

- `git diff --stat` since `58d1b86` (B54 reconcile): 13 NEW files under
  `.planning/case_profiles/case_004_v64_val_full_1_dicts/` (11) +
  `.planning/validation_reports/` (1) + `.planning/decisions/` (1)
- Concurrent sub-session B55: scope-disjoint in `ui/backend/services/`;
  no merge conflicts expected
- 0 routes/, 0 pages/, 0 ui/components/ files touched (surface-scan
  trailer optional per v2.3)
- 0 governance rule files touched
- 0 auth / signing / authorization boundaries crossed

## v2.3 compliance

| rule | compliance |
|---|---|
| DEC scope-driven; sub-DEC for single-axis work | ✅ sub-DEC under DEC-V64-A-charter; 6-field frontmatter present |
| Codex review on 1-sync-trigger (security boundary) | ✅ skipped; solver run + report not auth/signing |
| Kogami opt-in only (v2.3 round-1) | ✅ not invoked |
| Notion sync Accepted DEC at session-end | ✅ `notion_sync_status: pending` for main-session reconcile |
| Cadence floor 30 + counter as pure telemetry | ✅ counter not consulted; this is one sub-DEC in B56 |
| Confidence three-tier self-tag in each commit | ✅ all 3 commits include `confidence: med` |
| spike-class exclusion (≤30 LOC + 1 test, etc.) | ✅ NOT spike-class — multiple dict files + report + cross-cuts V-row analysis |
| Notion sync only Accepted DEC (round-1 loosen) | ✅ this DEC marked Accepted; main session syncs at end |
