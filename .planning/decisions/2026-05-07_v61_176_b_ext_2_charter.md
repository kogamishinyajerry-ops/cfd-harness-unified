---
decision_id: DEC-V61-176
title: B-ext-2 charter · F8 fix (async solve job lifecycle) — drive verdict pass ≥ 1/3 on DeepSeek cells (no budget cap)
status: Accepted
parent_dec: V61-162
phase: B-extend-2
notion_sync_status: pending
---

# DEC-V61-176 · B-Ext-2 Charter

## Status

**Accepted 2026-05-07** — Per B-extend close (DEC-V61-175) and user
explicit budget authorization ("no DeepSeek limit, push forward").

## Scope

Two atomic fixes + iterative live runs until charter target met.

| Sub-DEC | Capability |
|---|---|
| DEC-V61-177 | B-ext-2.1 persona prompt "Step 6: post-processing & verdict" lifecycle guidance |
| DEC-V61-178 | B-ext-2.2 R5 live re-run + iterate to verdict pass ≥ 1/3 |
| DEC-V61-179 | B-ext-2.3 close + Chinese delta summary |

## Charter goal

**verdict pass ≥ 1/3** on R5 (or R6+ if needed). B-extend close
demonstrated 2/3 cells reached Step 5 solve POST 200; remaining gap
is async-solve polling + result extraction. R5 with persona Step 6
prompt + max_steps=80 + max_input_tokens=3M closes that gap.

## Out of scope (B-ext-3 candidates)

- 6 deferred charter cells (Anthropic + gpt-5.4) — still gated on
  ANTHROPIC_API_KEY + CODEX_RELAY_API_KEY
- Workbench-side `/solve-sync` mode (charter §close §recommendations
  marked optional)
- Workbench taxonomy unification

## Termination criteria

- **HARD success**: any single live run submits a passing verdict
  (verdict.passed=True, observed within reference ± tolerance)
- **HARD success**: verdict pass ≥ 1/3 across the 3 DeepSeek cells
  (charter target met, B-ext-2 closes)
- **HARD bound**: if 5 R-iterations don't yield ≥1/3, escalate to
  user with diagnosis (probably workbench-side solve issue)

User pre-authorization: no budget cap. Iterate as needed.

## V130 contract

No new mutating routes. Persona prompt extension is content-only.
V132 contract test 21/21 must continue passing.

## Verification

- `pytest tests/dogfood/ ui/backend/tests/` 全绿 after each commit
- R5 (and any subsequent R-iterations) artifacts under
  `.planning/dogfood/runs/live_2026_05_07_r5*/`
- Aggregator-generated DOGFOOD_REPORT_LIVE_R5.md with severity table
- Final close summary documents which R-iteration first crossed
  verdict pass = 1 (and which cell)

## Counter

B-ext-2 increment: minimum +4 (charter +1, B-ext-2.1+2.2+2.3 +3).
Could be more if B-ext-2.2 splits into multiple R-iteration commits.

## References

- DEC-V61-175 · B-extend close
- `.planning/dogfood/B_EXT_CLOSE_SUMMARY_ZH.md` §F8
- `.planning/dogfood/runs/live_2026_05_07_r4b/` — R4.5 baseline trajectory
