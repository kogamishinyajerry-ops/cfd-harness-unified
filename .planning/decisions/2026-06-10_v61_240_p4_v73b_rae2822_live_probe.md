---
decision_id: DEC-V61-240
title: V73.B · RAE 2822 Case 9 live probe — converged transonic solve, anchors numerized, gate replays an HONEST ENFORCED CONFLICT
status: Accepted (autonomous under sponsor mandate 2026-06-10 「批准你继续开发优化」; civil-aircraft scope mandate)
parent_dec: DEC-V61-238 (V73.A scaffold: extractor + two-tier gate + gold skeleton)
phase: P4 V73.B (breadth anchor · coverage stays 3 — invariant pinned in gate output)
notion_sync_status: n/a (Notion retired, sponsor 2026-06-09)
autonomous_governance: true
confidence: high
date: 2026-06-10
codex_tool_report_path: reports/codex_tool_reports/2026-06-11_v73b_live_probe_R{0,1,2}.md
codex_review_relay: 86gs (gpt-5.4 xhigh)
---

# DEC-V61-240 · V73.B RAE 2822 Case 9 live probe

## TL;DR

First real transonic rhoSimpleFoam run in the repo: generator-authored RAE
2822 case at the AGARD AR-138 Case 9 corrected point (M=0.734, α=2.79°,
Re=6.5e6), converged (self-stopped, residualControl 5e-5) on ESI v2312
native arm64, frozen at `reports/showcase_aero/_v73b_rae2822_probe/` (SHA256
manifest). Tier-2 anchors numerized (Cl=0.803 ENFORCED 5% · shock_xc=0.525
ENFORCED ±0.05 · Cd=0.0168 ADVISORY). **The frozen verdict is tier-1
SANITY-PASS ×9 + tier-2 ENFORCED FAIL** — the gate honestly reports the
known vanilla-OpenFOAM aft-shock/high-lift bias (probe Cl 0.878 / shock
0.60). 14 replay tests pin verdict + numbers; opt-in live e2e mirrors
V71.A/B convention.

## Adjudications folded in (sponsor standing mandate)

1. **Operating point**: corrected (M=0.734, α=2.79) over nominal (0.730,
   3.19) — cross-confirmed as the free-air CFD convention by HiOCFD C2.2 +
   arXiv:1302.2132 + NPARC archive. B109's mixed form matches no source;
   superseded. Sponsor retains override (recorded in gold YAML).
2. **Anchor numerization**: per the PRE-REGISTERED V73.A criterion
   ("AGARD AR-138 … OR a trusted reproduction"): 3 independent secondary
   carriers agree → `anchor_verification: VERIFIED` with the honesty
   boundary explicit in provenance (primary PDF not directly digitized).
   shock_xc uses the TABULATED 0.525, not the ~0.55 prose figure.
3. **V73.A R2-fix de-facto verification**: the retro-queued question
   (Codex cap=3 overflow — R2 fixes prescription-aligned but unverified)
   is substantially answered: the live probe exercises the exact
   face-centre extraction path on 401 real surface points (206 upper / 195
   lower, contour split across the z=0-crossing aft camber, C6 cross-check
   1.0%). Replay tests freeze those numbers as the regression baseline.
4. **Capability-matrix correction**: the `naca0012_transonic ✅ PR` row was
   aspirational (no live transonic run ever existed). Corrected to
   rae2822_case9 (V73.B) with the honest-CONFLICT annotation; onboarding
   guide updated to match.

## The run (9 attempts — every failure is a pinned lesson)

| # | Change | Outcome → lesson |
|---|---|---|
| 1 | first mesh build | blockMesh `project` flips branch at x/c≈0.886 (cambered aft: upper-edge chord nearer LOWER surface) → 606 faces @ 89.8° non-orth. **Lesson: closest-point projection is branch-ambiguous on cambered airfoils; use spline-sampled polyLine** (max non-orth 63°, skew 2.5 OK) |
| 2 | first solve | SIGFPE in thermo lib @ iter 193 — vendor tutorial's `fvOptions limitTemperature` had been missed in extraction. **Lesson: vendor case = ALL its files** |
| 3 | vendor aerofoil relaxation | stationary period-~6 Cl limit cycle (±0.035, never converges) |
| 4 | U relax 0.9→0.5 (SIMPLEC) | cycle WORSE (std 1.4%→22%) — SIMPLEC assumes near-unity relaxation |
| 5 | + nNonOrthogonalCorrectors 1 | **converged** (956 iters, Cl std 0.07%) — the corrector, not the relaxation, was the cure |
| 6 | domain 15c→30c | measured probe α bias 0.217°→0.114° (scales 1/R as predicted) → C0b passes with margin; probe writeInterval 100→1 (convergence can stop at any iter) |
| 7 | farfield k/ω: derived (ν_t/ν=1, ω=61k) → vendor (k=0.01, ω=10) | derived ω gave 4.6 cm turbulence decay length (seed dead 30c upstream); vendor values keep it alive (278 m). Same converged answer — BL was already turbulent (max ν_t/ν=2219); deviation removed anyway |
| 8 | wrap 200→320 cells | shock 0.598→0.6005, Cl 0.865→0.878: **aft shock is grid-converged, not under-resolution** → frozen |
| 9 | A/B: vendor non-transonic profile + corrector | crawls at 0.4→0.05 iter/s (impractical); earlier limit-cycle mean Cl≈0.817 closer to anchor but UNCONVERGED. **Freezing an unconverged closer answer would be oracle-gaming; the converged honest miss is frozen** |

## The honest finding (RESULT.md §3)

Converged vanilla rhoSimpleFoam+SST: Cl 0.878 / shock 0.60 vs experiment
0.803 / 0.525 (structured codes: 0.78–0.81 / 0.50–0.53). ΔCl≈0.06 between
pressure-term formulations (transonic-SIMPLEC vs SIMPLE limit-cycle mean)
exceeds the anchor tolerance — discretization sensitivity is first-order on
this case. The two-tier gate does exactly its job: SANITY floor passes,
anchor judgment says CONFLICT. Follow-ups (V-series intel, not this slice):
rhoCentralFoam LTS (the gold's pinned fallback), Cp-profile-band QoI.

## Seam fix under the 批17 rule (gate 接受集 == 生产集)

The gate's provenance meta-gate consumes `source` + `digitization` keys;
the first YAML draft invented `benchmark`/`carriers` names → silent
PROVISIONAL downgrade caught in live replay. YAML aligned to the gate (the
reviewed contract); gold test now pins the gate-consumed keys explicitly.

## 四问门控

- **LLM 离线可跑**: YES — generator/extractor/gate are stdlib+yaml; no LLM.
- **artifacts**: YES — frozen probe bundle + SHA256SUMS + REPRODUCE/RESULT.
- **TrustGate 解释**: YES — every tier-1 leg + tier-2 candidate named in the
  summary string with numbers; CONFLICT is explained, not just flagged.
- **AI advisory-only**: YES — gate verdicts are computed, not narrated.

## 验证

- `pytest -q tests/p4/ --ignore=tests/p4/test_bfs_lowre_wiring.py
  --ignore=tests/p4/test_supersonic_wedge_taskrunner.py` → **189 passed,
  3 skipped** (14 new replay tests + 1 opt-in live skip; the two ignored
  modules are the PRE-EXISTING notion_client import fragility,
  V72A-FOLLOWUP-1 class — untouched by this slice).
- 3 stale pins updated to the new gold state with equal rigor (gold ×2 +
  gate PROVISIONAL→ENFORCED pin).
- Frozen-bundle manifest: `shasum -a 256 -c SHA256SUMS` → clean.
- Gate replay on bundle: SANITY-PASS ×9, tier2 ENFORCED, tier2_passed False.
- `lint-imports` unavailable in this checkout env (same fragility class);
  no `src/` file is modified by this slice → four-plane contract unaffected.

## Rollback

Single-commit revert; the probe bundle and gold flip are additive. V73.A
scaffold (DEC-V61-238) unaffected.
