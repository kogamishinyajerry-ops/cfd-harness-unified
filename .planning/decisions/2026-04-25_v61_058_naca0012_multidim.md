---
decision_id: DEC-V61-058
title: NACA0012 airfoil multi-dim validation · Type II 5-row schema · Ladson 1988 anchors · case 5 · v2.0 third-apply
status: METHODOLOGY_COMPLETE_PHYSICS_FIDELITY_GAP_DOCUMENTED (2026-04-25 · Stages 0+A+B1+B2+C+D+E code-complete · Codex pre-Stage-A REQUEST_CHANGES → R1 CHANGES_REQUIRED → R2 APPROVE_WITH_COMMENTS · Stage E live OpenFOAM sweep at α∈{0,4,8}° solver-converged · methodology-quality smoke + sanity PASS · physics-fidelity gates FAIL on under-resolved 16k-cell mesh · post-R3 defect addendum recorded per RETRO-V61-053 protocol · mesh refinement deferred to follow-up DEC)
supersedes_gate: none
intake_ref: .planning/intake/DEC-V61-058_naca0012_airfoil.yaml
methodology_version: "v2.0 (third-apply, post-V61-053 first-apply + V61-057 second-apply)"
case_type: II  # Codex F1 forced downgrade — gate set lacks 3 physically independent families
commits_in_scope:
  # Stage 0 — intake + Codex pre-Stage-A plan review
  - c323085 intake(naca0012): DEC-V61-058 v1 Stage 0 draft — 5-gate Type I plan
  - 44c7f6f intake(naca0012): DEC-V61-058 v2 — Codex F1-F6 verbatim edits applied
  # Stage A — gold YAML + CaseProfile (doc-only)
  - a889657 A1(naca0012): rewrite gold YAML — Ladson 1988 + Abbott + Gregory provenance
  - 5a80e4d A2(naca0012): expand CaseProfile to 5 registered risk_flags
  - a2b1d63 A1.fix1(naca0012): Codex round 1 stale physics_contract wording
  # Stage B1 — adapter α-routing + forceCoeffs/yPlus FOs
  - 5bbda96 B1.1(naca0012): α-routing in 0/U via task_spec.boundary_conditions
  - 032623b B1.2+B1.3(naca0012): forceCoeffs + yPlus FOs on aerofoil patch
  - a00ff88 B1.fix1(naca0012): Codex round 1 F1 BLOCKING — α-routing statefulness
  # Stage B2 — extractors + TestNACA0012MultiDim
  - 49395cf B2(naca0012): src/airfoil_extractors.py + TestNACA0012MultiDim
  - 641cf9f B2.fix1(naca0012): Codex round 1 F2 — NaN/inf guards in extractors
  # Codex curated review logs (R1 + R2 + R2-C4 cleanup)
  - 8bd3df4 docs(naca0012): curated Codex round 1 review log
  - 0fe6aba docs(naca0012): Codex round 2 C4 — cleanup residual stale α narrative
  - eb25892 docs(naca0012): curated Codex round 2 review log
  # Stage C — preflight + comparator gate wiring
  - 807c765 C(naca0012): preflight + comparator gate wiring [line-b]
  # Stage E plumbing fixes (live-run defects, RETRO-V61-053 verbatim-exception scope)
  - 2fe31eb B1.fix2+B2.fix2(naca0012): Stage E plumbing — sampledSurfaces list-form + forceCoeffs.dat fallback
  # Stage E live sweep + defect addendum
  - 56fe04f E(naca0012): Stage E live-run sweep + post-R3 defect addendum
  # Stage D — audit fixture regeneration
  - 893e52d D(naca0012): regenerated audit fixture under V61-058 multi-α gold
  # DEC closeout — this commit
  # commit hash assigned at commit time
codex_verdict: APPROVE_WITH_COMMENTS (round 2, Stages B1+B2+C scope). 3-arc Codex sequence — pre-Stage-A REQUEST_CHANGES (Stage 0 intake · 6 F1-F6 verbatim edits in 44c7f6f) → R1 CHANGES_REQUIRED (Stage A1+B1+B2 · 1 BLOCKING + 1 MED, both fixed verbatim a00ff88 + 641cf9f + a2b1d63) → R2 APPROVE_WITH_COMMENTS (Stage A1+B1+B2+C verify · 1 LOW C4 doc-sync, fixed 0fe6aba). Under F1-M2 strict, APPROVE_WITH_COMMENTS at R2 closes Stages 0-C as code-complete; Stage D + Stage E plumbing fixes + Stage E live sweep follow V61-057 precedent ("Stage E live run validated headline gate without further Codex review — operational not code review"). Post-R2 work is operational scope: (a) sampledSurfaces dict→list syntax fix (latent V61-044 bug, RETRO-V61-053 verbatim-exception scope), (b) forceCoeffs.dat filename fallback for OF10 (mirrors well-tested cylinder_strouhal_fft.py:373-385 from DEC-V61-041), (c) audit fixture regen (deterministic auto-generation), (d) Stage E live-run results documentation. None of these introduce new architectural surface area.
autonomous_governance: true
autonomous_governance_counter_v61: 42
external_gate_self_estimated_pass_rate: 0.40
external_gate_self_estimated_pass_rate_source: .planning/intake/DEC-V61-058_naca0012_airfoil.yaml §6
external_gate_actual_outcome_partial: |
  3-Codex-arc sequence (pre-Stage-A + R1 + R2), final R2 verdict APPROVE_WITH_COMMENTS. Pre-Stage-A pass rate ACTUAL=0% (REQUEST_CHANGES, 6 findings F1-F6 — case_type downgrade I→II + gold provenance pivot from Thomas/Lada to Ladson/Abbott/Gregory + 4 narrative tightenings) vs estimated 0.40 — overestimated due to underestimating the gate-independence rubric (3 physically independent families). All 6 verbatim edits applied in intake v2 (44c7f6f).

  R1 (Stage A1+B1+B2) ACTUAL=0% (CHANGES_REQUIRED, 1 BLOCKING + 1 MED). F1 BLOCKING surfaced a real correctness bug Codex independently reproduced via uv-run: persisted bc["alpha_deg"]=4 from first call masked second call's `angle_of_attack=8` alias change → cross-α leakage in serial driver. Fixed verbatim a00ff88 (reversed precedence to angle_of_attack-first, removed all stateful persistence, added fail-closed type check raising ParameterPlumbingError). F2 MED `math.isfinite()` guards landed 641cf9f. Codex's static-review catch on F1 is the strongest precedent yet for the "test multiple invocations in sequence" review pattern.

  R2 (Stage A1+B1+B2+C verify) ACTUAL=APPROVE_WITH_COMMENTS, 1 LOW (C4 stale α-routing narrative across 3 sites). Fixed verbatim 0fe6aba. R2 closed B1+B2+C as code-complete; Codex's C4 catch was helpful doc-sync hygiene.

  Stage D (audit fixture regen) ran phase5_audit_run.py → MISSING_TARGET_QUANTITY G1 fires correctly because phase5_audit_run.py is single-α and V61-058 introduced multi-α gold. Documented as architectural mismatch (multi-α audit pipeline support deferred).

  Stage E (live OpenFOAM sweep at α∈{0,4,8}°) — ALL THREE cases solver-converged in 17–31 s wall clock with residuals ~1e-7. Plumbing layer correct: extractors surface Cl/Cd/y+ into key_quantities, force_coeffs_source=forceCoeffs_FO_aerofoil for all 3, force_coeffs_FO + yPlus_FO + sampledSurfaces all fire correctly post-2fe31eb. Methodology-quality smoke assertions PASS:
    - sanity_cl_at_alpha_zero: |Cl|=7.2e-8 < 0.005 ✓
    - sign_convention_alpha_eight: Cl=0.49 > 0 ✓
    - α-routing produces correct U_inf_vec & liftDir/dragDir per α (no cross-α leakage; F1 fix verified by 3 independent solver runs)

  Hard physics-fidelity gates FAIL on the as-implemented mesh (16k cells, 120 airfoil surface faces, y+_max ≈ 139, domain ±5 chord):
    - HEADLINE Cl@α=8°: 0.491 vs gold 0.815 (40% under-prediction, tol 5%)
    - CROSS_CHECK Cd@α=0°: 0.020 vs gold 0.008 (151% over-prediction, tol 15%)
    - QUALITATIVE dCl/dα: 0.061 vs gold 0.105 (42% under-prediction, tol 10%)

  Defect category (RETRO-V61-053): solver_stability_on_novel_geometry — under-resolved boundary layer + wake. NOT methodology bug, NOT plumbing bug. All three failing gates trace to the same root cause: 120 airfoil surface faces (literature uses 200–400), y+_max ≈ 139 (high in log-layer where wall functions degrade), domain ±5 chord (literature uses ±20). Recommended remediation (out-of-scope for V61-058): airfoil surface points 120→300, near-wall layer for y+~30, domain extent ±5→±20 chord; estimated runtime impact 17 s → ~5 min per α at structured-grid kOmegaSST, push past intake §4 executable_smoke_test budget. Post-R3 defect classification: numerical_under_resolution (NOT accessor/dereference; the bug is real CFD physics from the adapter's default mesh template).

  Recommended follow-up: separate DEC (proposed V61-060) for high-resolution NACA0012 mesh profile, reusing Stage A gold YAML + Stage B extractors + Stage E driver verbatim. The V61-058 methodology layer (Type II 4-observable schema + α-routing + multi-α extractors + sign-convention smoke + sanity-check helper) is correct and ready for that follow-up to consume.
external_gate_caveat: "Type II DEC (1 PRIMARY scalar Cl@α=8° + 1 SAME_RUN_CROSS_CHECK Cd@α=0° + 1 PROFILE Cp_distribution + 1 QUALITATIVE dCl/dα + 1 PROVISIONAL_ADVISORY y+_max). HEADLINE FAIL on as-implemented mesh — under-resolved 16k-cell structured grid. SANITY_CHECK + sign-convention smoke PASS — methodology layer delivers correctly. Codex log archive: reports/codex_tool_reports/dec_v61_058_{plan_review,round1,round2}_review.md. Live-run sweep archive: reports/phase5_audit/dec_v61_058_live_summary.yaml + dec_v61_058_live_run_20260425T104455Z_alpha{0,4,8}.json."
codex_tool_report_path: reports/codex_tool_reports/dec_v61_058_round2_review.md (+ plan_review + round1 co-located)
notion_sync_status: "synced 2026-04-25T11:08 · Status=Accepted (page_id=34dc6894-2bed-8101-b3b0-c36ca99f7f1c, URL=https://www.notion.so/DEC-V61-058-NACA0012-Type-II-multi-dim-validation-methodology-v2-0-third-apply-34dc68942bed8101b3b0c36ca99f7f1c). Body: headline callout + 3-round Codex arc + Stage E sweep table + RETRO-V61-053 defect addendum + methodology v2.0 third-apply calibration table + counter calibration + GitHub artifact links + V61-060 follow-up callout."
github_sync_status: "merged 2026-04-25T11:04:57Z · PR #39 squash-merge commit 29a71e2 (18 commits on dec-v61-058-naca branch · all [line-b] tagged · OPS-2026-04-25-001 dual-track guard PASS · backend pytest PASS · frontend tsc/vite FAILURE pre-existing on main, unrelated to V61-058 content)"
related:
  - DEC-V61-053 (cylinder_wake Type I · v2.0 first-apply · risk_flag protocol origin)
  - DEC-V61-057 (DHC Type I · v2.0 second-apply · 4-round arc + Stage E precedent)
  - DEC-V61-044 (NACA surface sampler — sampledSurfaces dict-form bug origin, fixed in V61-058 2fe31eb)
  - DEC-V61-041 (cylinder forceCoeffs FFT — coefficient.dat/forceCoeffs.dat fallback precedent)
  - DEC-V61-036 (G1 MISSING_TARGET_QUANTITY — fires correctly on V61-058 audit fixture for single-α/multi-α mismatch)
  - DEC-V61-028 (Phase 5a audit pipeline — V61-058 audit fixture inherits V61-028 schema)
  - RETRO-V61-001 (autonomous governance counter de-coupled from STOP signal)
  - RETRO-V61-053 (post-R3 defect addendum protocol — V61-058 first new-DEC application)
---

## Stage 0 · Case Intake (F1-M1 v2.0 hard gate)

Signed intake: [`.planning/intake/DEC-V61-058_naca0012_airfoil.yaml`](../intake/DEC-V61-058_naca0012_airfoil.yaml).

Key determinations:

- **case_type = II** (Codex F1 forced downgrade — original I plan had 5 gates but Codex ruled the dependency graph collapsed: Cl@α=4 + Cl@α=8 + dCl/dα are not independent, all sit on the same lift-curve fit. Type II re-cast: 1 PRIMARY scalar + 1 SAME_RUN_CROSS_CHECK + 1 PROFILE + 1 QUALITATIVE + 1 PROVISIONAL_ADVISORY = 5-row Type II schema).
- **primary_gate_count = 1** (Cl@α=8° HEADLINE_PRIMARY_SCALAR; Cd@α=0° SAME_RUN_CROSS_CHECK; Cp_dist PROFILE; dCl/dα QUALITATIVE_GATE; y+_max PROVISIONAL_ADVISORY)
- **codex_budget_rounds = 4** soft target, **round 4 health check**, **round 6 force abandon** (F6-M1)
- **estimated_pass_rate_pre_stage_a = 0.40** (3 risk surfaces: gate-independence judgement, gold provenance pivot, α-routing statefulness)
- **case_id = naca0012_airfoil** (single canonical entry — no whitelist multi-entry hack)

**In-scope observables**: Cl@α=8°, Cd@α=0°, dCl/dα linear regime, Cp distribution at α=0°, y+_max @ α=8°.
**Out-of-scope** (enforced by §3b): stall regime, finite-AR effects, transition modeling, compressibility corrections.

**Gold provenance pivot (Codex F2 MED)**: original plan cited Thomas 1979 / Lada & Gostling 2007 (whitelist legacy entries). Codex F2 ruled these as "secondary aggregations" and required pinned pivots to primary references. Stage A1 commit a889657 swapped to:

- **Cl@α=8° = 0.815** — Ladson 1988 NASA TM-4074 §3.2 Tab.1 Re=3e6 (smooth airfoil, free transition)
- **Cd@α=0° = 0.0080** — Ladson 1988 NASA TM-4074 §3.2 Tab.1 Re=3e6 (same source)
- **dCl/dα = 0.105/deg** — Ladson 1988 §3.2 + Abbott 1959 Fig. 4-7 cross-check
- **Cp(x/c=0,0.3,0.5)** — Abbott 1959 NACA Theory of Wing Sections Fig. 4-7 (TE point dropped per Kutta condition)
- **y+_max ∈ [11, 500]** — Gregory 1970 ARC R&M 3726 advisory band

**Cl@α=0° EXCLUSION**: original plan included Cl@α=0° as a gate. Codex F4 MED + F5 MED ruled this an "SNR trap" because gold value is 0.0 for symmetric NACA0012 — any tolerance band evaluated with denom=0 yields ∞ relative error. Replaced with **SANITY_CHECK** using absolute band |Cl| < 0.005 (numerical noise floor), separate from the gate ladder. Live-run produced |Cl@α=0°| = 7.2e-8 — 5 orders of magnitude under the band — strongest possible methodology-layer signal.

**ADR-001 four-plane assignment**: `src/airfoil_extractors.py` registered in `src/_plane_assignment.py` as Plane.EXECUTION (alongside cylinder_strouhal_fft, naca_surface_sampler, plane_channel_uplus_emitter, wall_gradient_helper). All extractor imports are Execution → Execution (allowed).

## Stage A · Gold YAML + CaseProfile (doc-only)

- **A1** (a889657): rewrote `knowledge/gold_standards/naca0012_airfoil.yaml` from scratch — 4 observables + 1 provisional_advisory + 2 sanity_checks + 4 helper_reference_values blocks. `contract_status: PARTIALLY_COMPATIBLE` (preserves dashboard 5/1/1/1/2 bucket counts; `COMPATIBLE_WITH_KNOWN_DEVIATION` would have collided with the COMPATIBLE prefix bucket).
- **A2** (5a80e4d): expanded `.planning/case_profiles/naca0012_airfoil.yaml` to 5 registered risk_flags (executable_smoke_test, solver_stability_on_novel_geometry, python_version_parity, mesh_density_on_domain_change, numerical_noise_snr) + 6 case-specific intake-local pointers + 4-row tolerance_policy.
- **A1.fix1** (a2b1d63): Codex R1 F4 — refreshed 3 stale-narrative sites (gold YAML lines 31, 60; adapter line 7183-7184) to cite B1 commits + clarify only Stage E live-run validation remains.

## Stage B1 · Adapter α-routing + forceCoeffs/yPlus FOs

- **B1.1** (5bbda96): added `_generate_airfoil_flow` α-routing reading `boundary_conditions.angle_of_attack` → `alpha_deg`, threading into `0/U.fixedValue.uniform (cos α, 0, sin α)` and `controlDict.functions.forceCoeffs1.{liftDir,dragDir}` per ADR convention.
- **B1.2 + B1.3** (032623b): forceCoeffs FO + yPlus FO blocks added to `_generate_airfoil_controlDict_static_prefix`. `Aref = chord × thin_span = 1.0 × 0.002 = 2e-3`; `lRef = chord = 1.0`. yPlus writes patch-level min/max/avg every 10 steps.
- **B1.fix1** (a00ff88): Codex R1 F1 BLOCKING — reversed precedence to `angle_of_attack`-first, removed all stateful persistence (`bc["alpha_deg"]`, `bc["U_inf_x"]`, `bc["U_inf_z"]`), added fail-closed type check raising `ParameterPlumbingError` on bool/non-numeric. Codex independently reproduced the original bug via uv-run, demonstrating cross-α leakage between two consecutive `_generate_airfoil_flow` invocations.
- **B1.fix2** (2fe31eb, post-R2): `surfaces` dict → list syntax fix for OpenFOAM 10 sampledSurfaces. Latent V61-044 bug (commit a267d2a) — sampledSurfaces required `surfaces ( aerofoil { ... } );` not `surfaces { aerofoil { ... } }`. Surfaced when V61-058 first exercised the live path; never caught between V61-044 (Apr 22) and V61-058 (Apr 25) because no live-sweep exercised airfoil sampledSurfaces in that window.

## Stage B2 · Extractors + TestNACA0012MultiDim

- **B2** (49395cf): new `src/airfoil_extractors.py` (Execution Plane) — `compute_cl_cd` reuses `parse_coefficient_dat` from `cylinder_strouhal_fft` (DEC-V61-041 precedent), `compute_lift_slope` does closed-form OLS + linearity check (Cl(4) midpoint vs (Cl(0)+Cl(8))/2), `assert_sign_convention` for Stage E smoke. Per-gate SNR table embedded in module docstring.
- **B2.fix1** (641cf9f): Codex R1 F2 MED — `math.isfinite()` guards on Cl/Cd/y+ values raising `AirfoilExtractorError` with diagnostic context. NaN/inf from diverged solver runs would otherwise propagate as "trustworthy" gate values.
- **B2.fix2** (2fe31eb, post-R2): forceCoeffs.dat filename fallback for OpenFOAM 10. Mirrors the well-tested fallback chain in `cylinder_strouhal_fft.py:373-385` (`("coefficient.dat", "forceCoeffs.dat")`); compute_cl_cd previously hardcoded `coefficient.dat` only. New unit test `test_compute_cl_cd_accepts_forceCoeffs_dat_filename` covers the OF10 path. Tests now 40 (was 39).

## Stage C · Preflight + comparator gate wiring

- **C** (807c765): preflight gate registers V61-058's 5 risk_flags + 4 case-specific extractor names; auto_verify wires comparator role assignments per case_type=II tolerance policy. Stage C is a doc-grade integration commit (no Codex round needed).

## Stage D · Audit fixture regeneration

- **D** (893e52d): regenerated `ui/backend/tests/fixtures/runs/naca0012_airfoil/audit_real_run_measurement.yaml` via `EXECUTOR_MODE=foam_agent .venv/bin/python scripts/phase5_audit_run.py naca0012_airfoil`. Fixture now reflects V61-058 reality:
  - quantity: pressure_coefficient → lift_coefficient_alpha_eight
  - extraction_source: comparator_deviation → no_numeric_quantity
  - audit_concerns includes MISSING_TARGET_QUANTITY (V61-036 G1) + carry-over DEVIATIONS (V61-028 legacy Cp at α=0)
  - Verdict FAIL/FAIL preserved (expected/actual aligned)

  Architectural mismatch documented: phase5_audit_run.py drives a single-α run from the case_profile default (α=0); V61-058 introduced multi-α gold (HEADLINE Cl@α=8°) which cannot be measured at α=0. G1 fires correctly — the PASS-washing path closed by V61-036 stays closed. Multi-α audit pipeline support is a separate scope (proposed follow-up DEC).

## Stage E · Live OpenFOAM sweep

- **E** (56fe04f): driver `scripts/dec_v61_058_live_runs.py` ran 3 cases at α∈{0,4,8}° (timestamp 20260425T104455Z). All converged in 17.7 s / 19.0 s / 31.2 s wall with residuals ~1e-7. Cl/Cd/y+ surfaced via Stage C populator; force_coeffs_source=forceCoeffs_FO_aerofoil for all 3.
- **Smoke + sanity (intake §9) PASS**:
  - `sanity_cl_at_alpha_zero`: |Cl| = 7.2e-8 < 0.005 ✓
  - `sign_convention_alpha_eight`: Cl = 0.491 > 0 ✓
- **Hard gates (intake §3) FAIL**:
  | Gate | Role | Value | Gold | Tol | Outcome |
  |---|---|---|---|---|---|
  | Cl@α=8° | HEADLINE_PRIMARY_SCALAR | 0.491 | 0.815 | 5% | FAIL (40% under) |
  | Cd@α=0° | SAME_RUN_CROSS_CHECK | 0.020 | 0.008 | 15% | FAIL (151% over) |
  | dCl/dα | QUALITATIVE_GATE | 0.061 | 0.105 | 10% | FAIL (42% under) |
- **Post-R3 defect addendum** (RETRO-V61-053 protocol): `defect_addendum` block in `reports/phase5_audit/dec_v61_058_live_summary.yaml` records:
  - category: `solver_stability_on_novel_geometry`
  - classification: `numerical_under_resolution_NOT_methodology_bug`
  - mesh diagnostics: 16k cells, 120 airfoil faces, y+_max=139, max aspect ratio 108, domain ±5 chord
  - root cause: under-resolved BL + wake on as-implemented adapter mesh template
  - status_at_close: `known_limitation_documented`
  - recommended_followup: V61-060 high-resolution NACA0012 mesh profile DEC

## Codex round-by-round disposition

| Stage / Round | Verdict | Findings | Disposition |
|---|---|---|---|
| Pre-Stage-A plan review | REQUEST_CHANGES | 6 (F1 case_type II + F2 gold pivot + F3 SANITY rename + F4–F6 narrative tightenings) | All verbatim in intake v2 (44c7f6f) |
| Round 1 (A1+B1+B2) | CHANGES_REQUIRED | 1 BLOCKING (F1 α statefulness) + 1 MED (F2 NaN/inf) | Fixed verbatim a00ff88 + 641cf9f + a2b1d63 |
| Round 2 (A1+B1+B2+C verify) | APPROVE_WITH_COMMENTS | 1 LOW (C4 stale α narrative) | Fixed verbatim 0fe6aba |
| Stage E plumbing + live sweep | (no round — operational scope per V61-057 precedent) | 2 plumbing fixes (sampledSurfaces list, forceCoeffs.dat fallback) + post-R3 defect addendum | Documented in `defect_addendum` block of live_summary.yaml |

R1 BLOCKING merit deep-dive: Codex's static-review catch on cross-α statefulness is the strongest precedent yet for the "test multiple invocations in sequence" review pattern. Codex independently reproduced the bug (uv-run), demonstrating value beyond pattern-matching.

## Risk-flag close-out

| Risk flag | Status at close | Evidence |
|---|---|---|
| executable_smoke_test | RESOLVED | Stage E live sweep at α∈{0,4,8}° all converged in <1 min wall |
| solver_stability_on_novel_geometry | OPEN — documented in defect addendum | Mesh under-resolution causes hard-gate FAIL; methodology layer correct |
| python_version_parity | RESOLVED | All 40 tests pass on .venv (3.12); no f-string py3.9 escapes |
| mesh_density_on_domain_change | OPEN — documented in defect addendum | Same root cause as solver_stability above |
| numerical_noise_snr | RESOLVED | SANITY_CHECK |Cl@α=0°| = 7.2e-8 ≪ 0.005 — 5 orders of magnitude headroom |

## Methodology v2.0 third-apply calibration

| Dimension | V61-053 first-apply | V61-057 second-apply | V61-058 third-apply |
|---|---|---|---|
| Codex rounds | 4 (R1 → R4 APPROVE_WITH_COMMENTS) | 4 (R1 → R4 APPROVE_WITH_COMMENTS) | 3 (pre-A → R1 → R2 APPROVE_WITH_COMMENTS) |
| Pre-Stage-A review | not yet protocolized | informal | **REQUEST_CHANGES with 6 verbatim edits** |
| F1 forced case_type change | I confirmed | I confirmed | **I → II downgrade** |
| Live-run defects post-R3 | 6 | 0 | 2 (plumbing) + 1 (physics fidelity, addendum) |
| Headline gate verdict | precision-limited (St ΔSt~20%) | PASS (Nu +0.44%) | **FAIL (Cl 40% under)** |
| Methodology smoke verdict | n/a | n/a | **PASS (sanity + sign convention)** |
| Closeout grade | demonstration_grade | headline_validated | **methodology_complete_physics_fidelity_gap_documented** |

## Closeout disposition

V61-058 demonstrates the v2.0 methodology working end-to-end on a Type II case where the hard gates fail honestly. The methodology layer (4 observables × Type II schema, α-routing, multi-α extractors, sign-convention smoke, sanity-check helper, audit-fixture regen) delivered correctly through 3 Codex rounds and exposed an under-resolved-mesh signal that no synthetic test fixture would have surfaced. Per RETRO-V61-053 post-R3 defect protocol, the FAIL gates are recorded as a documented limitation with a follow-up DEC recommendation, NOT an arc retry.

The follow-up DEC (proposed V61-060) inherits the V61-058 methodology stack verbatim and only varies the adapter mesh template — that's the correct scope-split because mesh refinement is a separate decision (runtime budget, BC sensitivity, domain-extent tradeoffs) from validation methodology.
