---
decision_id: DEC-V62-A-sub-M-4Q-AUDIT
title: M-4Q-AUDIT · stack-level cross-feature four-question gate audit + LLM-offline acceptance suite · closes V62-A Tier 1 + Done dim #2
status: Accepted
parent_dec: V62-A-charter
phase: V62-A Tier 1 · M-4Q-AUDIT (closing milestone of Tier 1; unblocks Tier 2 stack-level Track C sessions)
notion_sync_status: pending
---

# DEC-V62-A-sub-M-4Q-AUDIT · stack-level 4Q audit

## Status

**Accepted 2026-05-14** — last sub-DEC under V62-A Tier 1. Aggregates the
per-module 4Q gate verifications that already live inline in
`test_advisor_stack.py`, `test_ai_review_route.py`, and
`test_ai_diagnose_route.py` into one signed cross-feature audit + a fresh
acceptance suite that exercises the LLM-offline contract over the live
HTTP surface.

## Goal

Close `DEC-V62-A-charter` Done Definition dimension #2 ("四问门控
cross-feature audit · stack-level audit + sign-off · LLM offline 全功能")
by producing:

1. A human-readable audit document with a 3-module × 4-question matrix
   citing the specific test that pins each cell.
2. A pytest acceptance suite that asserts the four questions over the
   live POST `/api/ai-review` and POST `/api/ai-diagnose` surface with
   `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` / `AZURE_OPENAI_API_KEY` removed
   from the environment.
3. A governance frontmatter chain (`parent_dec: V62-A-charter`) so the
   audit can be cited from the charter close DEC in Tier 3.

## Scope

### What this sub-DEC adds

- `.planning/audits/v62_stack_4q_audit.md` — aggregated audit document
  (3 × 4 matrix + acceptance-suite description + sign-off section
  pinning the 3 upstream sub-DECs' final LANDED commits as evidence).
- `ui/backend/tests/test_4q_gate_stack_acceptance.py` — 4-test acceptance
  suite (one per question):
  - `test_q1_llm_offline_both_routes_return_200_with_payload` — Q1
  - `test_q2_findings_carry_source_advisor_evidence_v_rows_and_audit_artifact` — Q2
  - `test_q3_trustgate_every_evidence_v_row_is_canonical` — Q3
  - `test_q4_case_dir_sha256_unchanged_across_both_routes` — Q4
- This DEC file.
- ARC-GOAL.md update: Tier 1 row `M-4Q-AUDIT` [ ] → [x], counter block
  Done dim #2 → MET ✓, last-updated trailer.

### What this sub-DEC explicitly does NOT add

- **No source modification to upstream modules.** `advisor_stack.py`,
  `routes/ai_review.py`, `routes/ai_diagnose.py` are read-only inputs to
  this audit. The audit's job is to verify what is already LANDED.
- **No new LLM import or call.** Adding any LLM dependency would
  invalidate Q1 by definition. The acceptance suite asserts absence,
  not presence.
- **No Codex relay invocation.** This work does not cross the V62-A
  charter's Codex-review trigger (`routes/ai_*.py` security boundary):
  it adds 1 test file + 2 docs files, touches zero route source. Opus
  self-assessed `confidence: med`; no v2.2 1-sync-trigger commit.
- **No new advisor or stack feature.** Advisor coverage widening (e.g.
  D9 / D10) is parked under V62-A Tier 2 and V63.
- **No Notion mutation.** Per v2.3 round-1 loosen, only Status=Accepted
  DECs sync to Notion, and that happens in the session-end batch — this
  sub-DEC's `notion_sync_status: pending` is the resting state for the
  duration of this commit chain.

### Surface-scan disposition (DEC-V61-088)

`grep -rin "v62_stack_4q_audit\|test_4q_gate_stack_acceptance"` returned
**0 hits** before this sub-DEC's first commit (the only prior hits were
forward references in `.planning/2026-05-14_v62_charter.md`,
`.planning/ARC-GOAL.md`, and `.planning/decisions/2026-05-14_v62_charter_dec.md`
that explicitly announced the upcoming artifact). No collision; no
pre-existing implementation to extend / parallel / refactor. Commit
trailer carries `Surface-scan: clean` per V61-088 convention.

## Verification matrix

| # | Verification | Evidence |
|---|---|---|
| V1 | Acceptance suite green | `pytest ui/backend/tests/test_4q_gate_stack_acceptance.py -q` → `4 passed in 0.44s` |
| V2 | No upstream regression | `pytest ui/backend/tests/test_advisor_stack.py test_ai_review_route.py test_ai_diagnose_route.py test_4q_gate_stack_acceptance.py -q` → `72 passed in 1.75s` |
| V3 | Q1 lever actually engaged | `test_q1_*` uses `monkeypatch.delenv` on all 3 LLM key env vars + asserts `llm_enhanced=False` + `llm_match_used=False` on responses |
| V4 | Q2 artifact actually persists | `test_q2_*` opens `audit_artifact_path` on disk and `json.loads` round-trips the report |
| V5 | Q3 trust anchor actually canonical | `test_q3_*` validates V-row ID ∈ `⋃ _V_ROWS_PER_ADVISOR.values()` AND ∈ `_V_ROWS_PER_ADVISOR[advisor]` |
| V6 | Q4 invariant actually byte-level | `test_q4_*` sha256s every file under `case_dir` pre + post both POSTs and asserts dict equality |
| V7 | Audit document signed | `v62_stack_4q_audit.md` §5 lists 3 evidence commits + this sub-DEC + signature line |
| V8 | ARC-GOAL counter updated | `ARC-GOAL.md` Tier 1 row + counter block + last-updated trailer reflect M-4Q-AUDIT closure + Done dim #2 = MET ✓ |

## v2.3 governance compliance

- **DEC scope**: sub-DEC (≤3 共享代码路径 — 1 test file + 2 docs; the audit
  doc + ARC-GOAL update are documentation paths, not shared
  implementation paths). Charter trigger (≥3 shared *implementation*
  code paths) does **not** fire. Parent DEC is `V62-A-charter`.
- **Codex review**: NOT triggered. Path does not cross any v2.2
  1-sync-trigger (auth / signing / security boundary). Opus
  `confidence: med` — Opus-autonomous self-merge per v2.2 confidence
  three-tier rule. The audit document itself cites Codex chains on the
  upstream 3 sub-DECs as evidence.
- **Round cap**: N/A (no Codex relay path entered).
- **Kogami**: opt-in only per V133; not invoked.
- **Cadence-floor THRESHOLD = 30**: 3 commits in this sub-DEC; well under.
- **Surface-scan trailer**: `Surface-scan: clean` (per V61-088).
- **Counter**: `autonomous_governance_counter_v61` +1 (pure telemetry).
- **Notion sync**: deferred; only Status=Accepted DECs sync, and that
  happens in the session-end batch per v2.3 round-1 loosen. Main
  session updates `notion_sync_status` from `pending` → `synced <date>
  (<url>)` at sync time.

## Confidence

`confidence: med` — the work is small (1 test file + 2 docs) and uses
patterns already established by the three upstream sub-DECs. Risk is
concentrated in the acceptance suite's monkeypatch chain (corpus cache
invalidation + repo-root redirection + env-var deletion all fan out
correctly); that risk is bounded by the 4-test green run plus the
72-test full-suite regression check.

## Open follow-ups (out of scope for M-4Q-AUDIT)

1. **Stack-level Track C** (M-STACK-TRACK-1/2/3, Tier 2) — re-runs the
   4Q gate against real industrial cases end-to-end. Any failure ratchets
   this audit with a §7 Findings & corrections appendix.
2. **Drift hook v2** (M-DRIFT-V2, Tier 2) — enforces V-series ↔ runtime
   corpus invariant at `/ai-review` boundary; the natural Q3 extension
   once corpus grows past ~100 rows.
3. **Diagnose surface widening** (future sub-DEC under V62-A or V63) —
   if `/ai-diagnose` gains shm/thermo/thin_wall discovery, the
   acceptance suite extends by one test and this audit is re-signed.

## End of sub-DEC
