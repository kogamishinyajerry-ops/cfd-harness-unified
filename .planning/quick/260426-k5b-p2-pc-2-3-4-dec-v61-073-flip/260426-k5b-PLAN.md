---
quick_id: 260426-k5b
title: P2 三件套 PC-2/3/4 + DEC-V61-073 flip to Accepted
date: 2026-04-26
mode: quick
constraints:
  - 不动 src/cfd_harness/trust_core/** (read-only; turned out to be src/metrics/ in actual codebase)
  - 132/132 trust-gate + 854 broader suite must stay green (turned out to be 860/1-flake/2-skipped at HEAD c65cbc7)
  - PC-2 / PC-3 / PC-4 each gets independent Codex APPROVE before DEC flip
must_haves:
  - PC-4: single canonical §10.5.4a 7-surface block; obsolete 5-surface duplicate gone; chronology bridges from old rules to current
  - PC-3: scripts/methodology/sampling_audit.py with ≤100k token cap, abort path emitting EXCEEDS_BUDGET_CAP=<u>/<c>, 7-surface grep coverage incl. correction_spec/ and .planning/case_profiles/, unit tests
  - PC-2: docs/specs/EXECUTOR_ABSTRACTION.md v0.2 with §5 hybrid-init OpenFOAM-truth invariant + §6 TrustGate executor-aware routing
  - DEC-V61-073 flipped from ACCEPTED_WITH_AMENDMENTS_PENDING_LANDING to Accepted, citing PC-2/3/4 final commits + 100k cap + 7 surfaces
  - Each PC reviewed independently by Codex; CHANGES_REQUIRED → fix → re-review until APPROVE
---

# 260426-k5b · P2 三件套 PC-2/3/4 + DEC-V61-073 flip

## Task

Land DEC-V61-073's 4 pre-conditions (PC-1 through PC-4), then flip the
DEC's status from `ACCEPTED_WITH_AMENDMENTS_PENDING_LANDING` to
`Accepted`. PC-1 is the flip itself; PC-2/3/4 are the work products.
Each gets its own Codex review before the DEC flip lands.

## Plan

### Task 1: PC-4 cleanup (lowest risk first)

- File: `.planning/methodology/2026-04-26_v2_section_10_5_and_11_draft.md`
- Action: remove the duplicate obsolete 5-surface §10.5.4a block; keep
  the canonical 7-surface version (DEC-V61-073 H1+A4 expansion).
- Verify: `grep -nE "^### §10.5.4a"` shows exactly one match.
- Done: file has single §10.5.4a section enumerating 7 surfaces.

### Task 2: PC-3 enforcement script + tests

- Files (new):
  - `scripts/methodology/sampling_audit.py`
  - `scripts/methodology/__init__.py`
  - `tests/methodology/test_sampling_audit.py`
  - `tests/methodology/__init__.py`
- Action:
  - Build `AuditWindow` dataclass + `build_audit_window` walker.
  - Token estimation: `tiktoken cl100k_base` if available, else
    `len(text) / 4` fallback.
  - Token cap: `DEFAULT_CAP_TOKENS = 100_000`; `--cap` and
    `SAMPLING_AUDIT_CAP` env override; raise-only enforced (exit 3 on
    lower).
  - Abort: `verdict == "EXCEEDS_BUDGET_CAP"` → exit code 2 + literal
    `EXCEEDS_BUDGET_CAP=<used>/<cap>` to stdout (and stderr in JSON
    mode).
  - 7-surface regex tuple covering all §10.5.4a surfaces incl. the new
    correction_spec/ + .planning/case_profiles/ paths.
  - Mixed-commit handling: include any commit touching non-trust-core
    files; use git pathspec exclusion to drop trust-core hunks from
    the diff load.
  - Auto-range fallback: try `main` → `HEAD`; cap lookback at
    `min(50, total-1)`; single-commit repo → plain `HEAD`.
- Verify: `pytest tests/methodology/test_sampling_audit.py -v` → all
  pass; smoke run `python -m scripts.methodology.sampling_audit
  --range HEAD~5..HEAD` returns OK with surfaces 6+7 detected.
- Done: 24 tests pass; full suite has +24 tests vs baseline.

### Task 3: PC-2 spec doc

- File (new): `docs/specs/EXECUTOR_ABSTRACTION.md` (v0.2)
- Action: Author repo-local canonical mirror of Notion EXECUTOR_ABSTRACTION
  doc with:
  - §5 hybrid-init OpenFOAM-truth invariant (formal byte-equality of
    canonical_artifacts, surrogate non-canonical exclusion,
    MODE_NOT_APPLICABLE escape).
  - §6 TrustGate executor-aware routing (per-mode verdict ceilings:
    docker_openfoam = full triad, mock = WARN ceiling, hybrid_init =
    defer to OpenFOAM artifact, future_remote = stub).
  - Vocabulary aligned with actual `MetricStatus` enum
    (`PASS / WARN / FAIL`); brief's "PASS_WITH_DISCLAIMER" shorthand
    explained as `WARN` + note string.
  - canonical_artifacts surface list aligned with actual
    `manifest.py:_RUN_INPUT_FILES` (9 inputs) + `_INITIAL_FIELD_FILES`
    (8 field files) + actual outputs (no fabricated `final_residuals`
    top-level field).
  - §6.4 routing logic correctly placed in `src/metrics/` (Plane.EVALUATION
    per ADR-001 line 73), not the non-existent `src/cfd_harness/trust_core/`.
- Verify: file exists; sections §1-§8 present; cross-references resolve.
- Done: spec doc lands.

### Task 4: Codex review per PC

- Run `cx-auto 20 && codex exec "<focused review prompt>"` for each PC
  in turn. On CHANGES_REQUIRED, address findings and re-review.
- Verify: each PC ends with APPROVE.
- Done: 3 APPROVE verdicts in hand.

### Task 5: DEC-V61-073 flip

- File: `.planning/decisions/2026-04-26_v61_073_independent_audit_amendments.md`
- Action: update frontmatter (`status: Accepted`, add
  `pc_closure_commits` map + `external_gate_actual_outcome`); append
  "PC closure addendum" section to body documenting each PC's commit
  and Codex round count.
- Verify: status is `Accepted`; addendum cites PC-2/3/4 commits + 100k
  cap value + 7-surface enumeration; commit-trailer carries `Notion-DEC:
  V61-073 Accepted` + `Notion-Tracker: PC-2=GREEN, PC-3=GREEN,
  PC-4=GREEN, §10.5+§11=Active` + `Signed-off-by: CFDJerry`.
- Done: P2-T1 unblocked.

## Constraints accepted

- All edits stay outside `src/cfd_harness/trust_core/**` (which doesn't
  exist; actual trust-core 5 modules are read-only):
  `src/gold_standards/`, `src/auto_verifier/`, `src/convergence_attestor.py`,
  `src/audit_package/`, `src/foam_agent_adapter.py`.
- Full pytest suite must not regress vs baseline (860 passed at session
  start commit `c65cbc7`). The single pre-existing flaky test
  (`test_build_trust_gate_report_resolves_display_title_to_slug`,
  passes isolated, fails under full-suite ordering) is unrelated to
  this PC arc.
- Each PC commits atomically; revisions per Codex round are separate
  commits per CLAUDE.md "always create new commits, never amend".
