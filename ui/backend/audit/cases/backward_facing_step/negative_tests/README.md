# Negative tests — backward_facing_step

Phase 0 placeholder. Seeded negative-test fixtures (manifests designed
to fail specific gates) will be added when the V&V infrastructure is
mature enough to validate them without polluting the canonical case.

See `tests/test_red_team_safety.py` for the harness-level negative
tests that fence trust-gate honesty rules independent of any case.
