"""cfdtrust — AI-CFD-V2 Phase 0 trust harness.

Public surface is intentionally small. The trust loop is exposed via the CLI
(`python -m cfdtrust.cli`) and the audit modules in `cfdtrust.audit`.
"""

__version__ = "0.0.1"
