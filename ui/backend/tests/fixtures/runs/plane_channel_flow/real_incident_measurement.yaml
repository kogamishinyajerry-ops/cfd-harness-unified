# Real-incident run for plane_channel_flow. §5d Part-2 originally
# reported comparator_passed=true at 5% tolerance, but the quantity
# returned is `U_max_approx` (scalar shortcut) whereas the gold expects
# `u_mean_profile` (vector). Post DEC-V61-025 engine fix, the contract
# engine correctly flags this as a quantity mismatch → FAIL. This is the
# intended behavior: a scalar fallback cannot validate a profile-shaped
# gold, and silently PASSing would be a silent-pass hazard.
run_metadata:
  run_id: real_incident
  label_zh: 真实运行 · 标量退化比较 (FAIL — 引擎正确识别 quantity 不匹配)
  label_en: Real run (scalar-fallback comparison — correctly FAILs)
  description_zh: >-
    Channel flow 的 gold 是整条 u_mean(y+) 剖面（向量），但 adapter 当前
    只能输出 U_max 标量。引擎修复后（DEC-V61-025）正确识别这是 quantity
    不匹配，判 FAIL——这是预期行为：scalar fallback 不能验证 profile-shaped
    gold，静默 PASS 反而是 silent-pass hazard。要真通过需要 DNS-grade
    剖面采样，会在 Phase 5b 真值化时补上。
  category: real_incident
  expected_verdict: FAIL
case_id: plane_channel_flow
source: p2_acceptance_solver_run
measurement:
  value: 1.112475
  unit: dimensionless
  run_id: p2_acc_plane_channel_flow_b8be73a
  commit_sha: b8be73a
  measured_at: '2026-04-20T13:28:57Z'
  quantity: U_max_approx
  extraction_source: key_quantities_fallback
  solver_success: true
  comparator_passed: true
audit_concerns:
  - concern_type: CONTRACT_STATUS
    summary: 'Quantity: u_mean_profile | Tolerance: 5.0% | PASS (scalar fallback)'
    detail: 'Quantity: u_mean_profile | Tolerance: 5.0% | PASS'
    decision_refs:
      - DEC-V61-019
decisions_trail:
  - decision_id: DEC-V61-019
    date: '2026-04-21'
    title: PR-5d.1 Codex closure (HIGH+MED)
    autonomous: true
