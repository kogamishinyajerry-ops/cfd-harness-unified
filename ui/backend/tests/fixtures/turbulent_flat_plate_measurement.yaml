# Curated fixture for Screen 4, updated with §5d Part-2 real-solver measurement.
# Sources:
#   - DEC-ADWM-005 (EX-1-009 Spalding fallback audit wiring — curated audit narrative)
#   - DEC-V61-019 (PR-5d.1 Codex closure — governance context)
#   - RETRO-V61-001 (v6.1 governance retrospective — new turf rules)
#   - DEC-V61-020 (PR #20 docker dep + error-message honesty — enabled the batch run)
#
# Measurement value from FoamAgentExecutor batch run on main SHA b8be73a.
# Run id / commit_sha / measured_at reflect the §5d Part-2 batch.
# Audit concern narrative preserved from the original DEC-ADWM-005 curation
# because it is still semantically correct: the 0.007600... value IS the
# Spalding log-law reference returned by the fallback path, not a raw solver
# extraction. The curated COMPATIBLE_WITH_SILENT_PASS_HAZARD concern is
# therefore the right regulatory-review framing for this measurement.

case_id: turbulent_flat_plate
source: p2_acceptance_solver_run_curated_narrative
measurement:
  value: 0.007600365566051871
  unit: dimensionless
  run_id: p2_acc_turbulent_flat_plate_b8be73a
  commit_sha: b8be73a
  measured_at: "2026-04-21T05:45:00Z"
  quantity: cf_skin_friction
  extraction_source: comparator_deviation
  solver_success: true
  comparator_passed: false
  spalding_fallback_activated: true

audit_concerns:
  - concern_type: COMPATIBLE_WITH_SILENT_PASS_HAZARD
    summary: >-
      Spalding fallback substitution armed for Cf>0.01; producer flag
      `cf_spalding_fallback_activated` asserted on this run.
    detail: >-
      src/foam_agent_adapter.py substitutes a Spalding log-law value
      for Cf when the raw solver-derived number exceeds 0.01. EX-1-009
      (DEC-ADWM-005) added producer flag emission + consumer enrichment
      so the audit_concern is suffixed with `:spalding_fallback_confirmed`
      for reviewer transparency. Measured Cf=0.007600... matches the
      Spalding reference precisely — the source is the fallback path,
      not direct solver extraction. Note that P6-TD-002 tracks the
      observation that duct_flow returns the same Spalding value to 10
      decimal places, suggesting the fallback is case-parameter-
      independent and needs cross-case audit.
    decision_refs:
      - DEC-ADWM-005
      - DEC-V61-020

decisions_trail:
  - decision_id: DEC-ADWM-005
    date: "2026-04-18"
    title: "EX-1-009 Spalding fallback audit wiring self-APPROVE"
    autonomous: true
  - decision_id: DEC-V61-020
    date: "2026-04-21"
    title: "PR #20 docker dep + FoamAgent error-message honesty (Codex round 6)"
    autonomous: true
  - decision_id: RETRO-V61-001
    date: "2026-04-21"
    title: "v6.1 governance retrospective (bundle D)"
    autonomous: true
