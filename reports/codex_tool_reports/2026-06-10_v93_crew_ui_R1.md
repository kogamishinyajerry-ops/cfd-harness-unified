# Codex review · V93 Agent Crew Observatory · R1 (fix commit 5152800)

- date: 2026-06-10
- relay: 86gs (gpt-5.4 xhigh) · `codex review --commit 5152800`
- DEC: DEC-V61-239
- verdict: **CHANGES_REQUIRED** (1×P2 — adopted)

## Finding (verbatim)

- [P2] Treat directory report refs as missing —
  ui/backend/services/agent_crew.py:312-318
  When `codex_tool_report_path` points at a directory with an inline
  annotation, `_clean_report_refs()` now strips the annotation and leaves a
  real directory path behind. `DEC-V61-095` currently uses
  `reports/codex_tool_reports/m_render_api_arc/ (...)`, so `exists()` passes
  here and `read_text()` raises `IsADirectoryError`, turning
  `/api/agent-crew/arc/DEC-V61-095` from a handled `MISSING` state into a
  500. Please require `abs_path.is_file()` (or otherwise treat directories
  as missing) before reading.

## Adoption

`exists()` → `is_file()` at all three read sites (frontmatter path-a, glob
path-b now also skips subdirectories, arc-detail re-check). Regression test:
DEC-V61-095-style annotated directory ref → round missing=True + arc detail
returns without raising. backend 20 passed.
